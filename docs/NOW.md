# NOW — hs/ 감시가 «제일 먼저» 읽는 파일
(크론 본문의 항목번호보다 이게 우선이다. 축을 띄울 때마다 여기를 갱신한다.)

## 지금 도는 축 — V-227 「자가 굽은 복도에서 도착을 포기한다」
STARTED=2026-09-01 21:12 · PID 5319 · ETA ~21:50
  로그 `~/.openclaw/workspace/tmp/necro_hs_v227_prog.log[.done]` · per-cell `tmp/hs_v227_prog.json`
  러너 `tools/hs_v227_prog.sh` (V227_ARMS=1) · 되돌림 `NAV_PATHPROG=0`
  두 팔 = BEFORE 옛 직선거리 진척 / AFTER 길 위 남은 거리 — **게임 손잡이·간선 규칙은 전부 현재 바이너리로 고정**

  왜: 층5 에서 이동시간 100% · 269px/s 로 17464px 를 걷는데 방은 2/14(거리/방 8732).
      층1 은 같은 속도로 거리/방 1051 · 8/8. 먼 게 아니라 **도착 판정이 접는다.**
      진척을 「목표 중심까지 직선거리」로 재니 굽은 복도에서 4초 무진척이 늘 발화 → 8초 블랙리스트 → 딴 방 → 반복.

  **끝 조건: 층5 거리/방 ≤1600 · 층5 방문 ≥50% · 층5 면적 ≥8%
            회귀 = 완주 137~319s · 오류 0 · 몸벽밖 0%**

## 다음 감시가 할 일 (순서대로)
1. `necro_hs_v227_prog.log.done` 이 있으면 → per-floor 로 갈라 **층5 거리/방·방문·면적**을 본다
   (`python3` 로 `tmp/hs_v227_prog.json` 의 runs[].cells 를 층별 중앙값). 채우면 V-227 을 닫는다(ROADMAP·NOW 둘 다).
   ★ 닫히면 **밴드/죽음 눈금을 통째로 다시 박는다** — V-226 의 33.3%/53.3% 는 «안 걷는 자»로 잰 값이다. 그게 곧 다음 축.
   ★ 미달이면 → 포기 규칙의 남은 두 자리를 본다: 끼임 2연속 시 12초 블랙리스트(과했다) · 웨이포인트 도달 반경 56px.
2. `.done` 이 없으면 → PID 5319 살았는지 보고 상태 한 줄. **다시 띄우지 말 것.**
3. 그 뒤 대기: V-224(발사체 벽밖 5~9.6%).

## 닫힌 것 — 다시 집지 말 것
- **V-226** 닫음 (`c64a282`). 깊이 곡선을 떼니 층5 압박비 5.97 → 1.01.
- **V-225** 닫음 (2026-09-01 21:10). 간선 규칙은 고쳤고(순수 그래프 성분>1 10.7%→0%) **기본으로 남겼지만**,
  게임 안에서는 두 팔 다 경로없음 0 이라 **「자가 안 걷는」 원인이 아니었다**. 되돌림 `NAV_LEGACY=1`.

## V-226 확정 눈금 (안 걷는 자로 잰 값 — V-227 이 닫히면 무효)
  교전층 15/15 · 밴드 33.3% [≥25%] · 죽음 53.3% [5~20%] · hp최저 중앙 5% · 완주 261.4s
  압박비 = 실제맞음 ÷ (maxhp ÷ (적dmg×16)) · 층별 1.09/1.24/0.49/0.17/1.01 · 되돌림 `__V226B=false`

NOTE=병수님 07:23 「긴장은 니가 알아서 만들어라 왜 자꾸 나한테 정하래」 — 값은 재서 정하고
  되돌릴 손잡이만 남긴다. 「선택 대기」 금지.
