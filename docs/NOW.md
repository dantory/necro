# 지금 도는 일 (감시가 제일 먼저 읽는 파일)

감시 크론의 지시문은 **낡을 수 있다.** 이 파일이 최신이다.

## ✅ V-201 — 지형지물 무시 이동 : **닫음** (2026-08-31 12:40, 커밋 578c5e3/45b2f70)
병수님 「맵의 지형지물을 다 무시하고 이동된다」(08-31 11:35).
걸을 수 있는 자리 = 방 ∪ 복도. stepTo 축분리 미끄러짐 + unstick.
자 `tools/hs_v201_walk.mjs` — 벽밖 **63.58% → 0%** · 소품겹침 1.07% → 0% ·
끼임창 0(최대정지 5f) · compute p95 2.6ms. 컷 넷 눈으로 확인 · 보고 완료.

## ✅ V-202 — 맵 구조를 BSP 로 : **닫음** (2026-08-31 12:56, 커밋 da85f49)
병수님 「맵도 좀 이상하고」(08-31 11:36). `genFloor` 를 무작위 뿌리기 → BSP 재귀분할로 다시 씀.
자 `tools/hs_v202_map.mjs` (씨앗 8 × 층 1·5·10):
관통복도 1.63→**0** · 방넓이 최대/최소비 ~2.0→**5.3(층1)·7.6(층10)** ·
바닥비율 36.9→16.7% → **40.2→48.4%** · 방개수 9→14 → **8→25** · 도달률 100% · ≤2ms/층.
조감도 셋 + 게임컷 둘 눈으로 확인 · 13:05 보고 완료.

## ▶ V-202b — 밸런스 (돌고 있음)
소환 자리가 8 → **98** 까지 불고 한 층에서 **2000 마리**를 죽인다. 피해가 여섯 자리.
「한 판이 어떤 모양인가」를 재는 자가 **아예 없다** — 여태 자는 전부 그림만 봤다.

- 지시문: `/Users/lbs/.openclaw/workspace/tmp/hs_v202b_prompt.md`
- 로그: `/Users/lbs/.openclaw/workspace/tmp/necro_hs_v202b.log`
- 마커: 같은 경로 `.done` (내용이 exit code · **124 면 시간초과**)
- 띄운 때: 2026-08-31 13:05 · timeout 3000s (≈50분)

**깨어나면:** 마커를 본다.
- 있고 rc=0 → `git status` 로 **커밋됐는지** 확인(안 됐으면 검증하고 커밋),
  `tmp/hs_v202b_*.png` 를 **직접 열어** 보고 before/after 수치와 함께 병수님께 보고.
- 있고 rc≠0(특히 124) → 작업 트리에 남은 변경을 **직접 검증·마무리·커밋**한다.
  V-201·V-202 도 시간초과로 끝났는데 코드는 멀쩡했다 — 마커만 보고 버리지 말 것.
- 없으면 → 상태 한 줄만 적고 끝낸다(노이즈 금지).
