# 지금 도는 일 (감시가 제일 먼저 읽는 파일)

> ⚠️ **크론 지시문이 낡았다(2026-08-31 18:00).** `necro-work` 크론 본문은 아직
> 「지금 도는 일: V-201」이라고 적혀 있지만 **V-201·V-202·V-202b·V-203·V-204 는 다 닫혔다.**
> 크론 본문 말고 **이 파일을 믿는다.**
> **크론 본문은 감시 턴에서 못 고친다(2026-08-31 18:33 확인)** — `necro-work` 크론이 띄우는
> 세션에는 OpenClaw `cron` 도구가 정책상 안 붙는다(붙는 건 CronList/CronDelete 로, 별개 계통이라
> 이 크론이 안 보인다). **병수님과의 보통 턴에서** `cron` 으로 본문을 갱신할 것.

## ✅ V-210 끝 — 잠든 무리가 개체로 읽힌다 (2026-09-01, 커밋 후·눈 판정)
잠든 팩이 스폰 자리에 몸이 겹친 채 굳던 것(separateEnemies 가 «깨어 있는» 팩만 밀어)을 고쳤다.
**싼 길 ㉠** — `hs/map.js` `spreadPack()` 를 스폰 때 한 번 팩 안에서만 돌린다. 잠든 팩은 안
움직이니 그 한 번으로 끝나고, **런타임 비용 0**(핫패스에 안 붙는다). RNG 를 안 건드려 같은 씨앗
맵 생성 순서는 그대로.
**자(hs_v207_walk, seed 1337·WAKE 500·walk3 구도) 앞뒤 동일:** frame p95 50.1→50.1ms · 벽밖 0% ·
콘솔오류 0 · walk3 동시적 22/무리 2. **눈 판정(`tmp/hs_v210_{before,after}.png`):** before 는
아래쪽 스무 마리가 한 죽처럼 세로로 뭉쳐 안 세어졌고, after 는 바닥 띠로 퍼져 각자 제 고리를
두르고 개체로 읽힌다 — 통과.

## ▶ 지금 도는 일 — **V-211: 글자가 그림을 덮는다** (2026-09-01 02:05 띄움)
V-210 닫힘. 다음으로 **축을 옮겼다** — V-201~V-210 이 여섯 판 내리 적 밀도·충돌·WAKE 축만 팠고
([[watchdog-digs-one-axis]]), 밸런스 축은 V-203b 에서 **자가 창보다 4~5배 거칠어** 막혀 있다.
그래서 **눈으로 바로 판정되는 HUD/글자 축**으로 간다.
집은 것: `docs/ROADMAP.md` 의 **「## V-200 후보」** 셋 — 2026-08-31 11:30 감시가 컷을 열어 찾아
적어 뒀는데 V-201(병수님 지적)이 끼어들어 **한 번도 안 고쳐진** 자리다.
· ㉠ 떠오르는 피해수가 한 줄에 다섯 겹쳐 안 읽힘(가로 충돌 검사 없음) — 통과선 겹친 쌍 0
· ㉡ 상태 글자가 왼쪽 아래 여섯 겹으로 쌓여 툴팁을 덮음 — 통과선 «×N» 묶기
· ㉢ 적 이름표가 폭발을 덮음(세 겹) — 고치기 전 값부터 잴 것
지시문 `tmp/hs_v211_prompt.md` · 로그 `/Users/lbs/.openclaw/workspace/tmp/necro_hs_v211.log`
· 마커 `…log.done` · 컷 `tmp/hs_v211_{before,after}*.png`

**다음 감시가 할 일:** `.done` 이 있으면 커밋 여부를 보고, 컷을 **직접 열어** 세 눈금을 판정해
병수님께 보고한다. 없으면 상태 한 줄만 적고 끝낸다.

## ★★ 병수님 17:34 「알아서 해라」 — **취향을 되묻지 말고 내가 정한다**
V-203 의 「어느 쪽으로 아프게 할지」를 여쭀더니 이 답이 왔다. **결정은 이쪽 몫이다.**
· **정한 것: `__RANGED_MOB` 켬 + 사람 hp 대비 적 피해 상향.** 근거는 V-203 표다 —
  원거리만 소환수 벽을 **원리로** 넘고(맞은수/초 0.8 · 나머지 0.1~0.2), 그런데 어느 팔도
  hp 를 못 깎는다(hp 4,515 인데 적 피해 6~18). **닿게 하는 팔 + 아프게 하는 팔이 둘 다** 있어야 한다.
· 앞으로도 **값·연출·구성의 취향 문제는 묻지 말고 정하고 알린다.** 되돌릴 수 있게
  손잡이로 만들어 두고, 「이렇게 정했고 근거는 이 수다」로 보고한다.
  물을 것은 **되돌리기 비싼 갈림길**뿐이다(장르를 바꾼다든가).

## ▶ V-203b — 위 결정을 실제로 넣는다 : **코드 커밋됨(검증 전) · 스윕 도는 중** (2026-08-31 18:08 띄움)
⚠️ 18:02 에 띄웠다던 잡은 **죽었다**(로그·`.done`·코드 변경이 하나도 안 남았다 — 옛 「띄웠는데
안 돌던」 패턴). 18:08 에 다시, 이번엔 코드부터 커밋하고 스윕을 띄웠다.

**한 일(커밋 352ddea, 검증 전):** `hs/main.js`
㉠ `__RANGED_MOB` 기본 켬(`=== undefined` 이면 true · 끄려면 `globalThis.__RANGED_MOB=false`).
㉡ 적 피해는 **hurtPlayer 한 곳**에서만 `FOE_DMG_MUL`(기본 24, `__FOE_DMG` 로 되돌림) 로 키운다 —
   `m.dmg` 는 안 건드려 소환수 vs 적 밸런스 유지 · 층 깊이 성장 축(scale)은 그대로 두고 그 위에 곱.

**지금 도는 것:** `tools/hs_v203b_sweep.sh`(→ `tools/hs_v203b.mjs`) 가 before(mul 0)+곱 16/24/32 를
씨앗 1,2,3 로 잰다. 로그 `/Users/lbs/.openclaw/workspace/tmp/necro_hs_v203b_sweep.log`,
끝나면 `…sweep.log.done`(종료코드). 결과 JSON `tmp/hs_v203b_{before,m16,m24,m32}.json`(각 `row.pass`).

**끝 조건(자로 판정):** hp<50% 층 ≥30% · 죽은 층 5~20%(가끔 죽어야 벽이다) · 완주 규격 안(~137s) · 오류0.
자·봇·지표는 V-203 과 같다(dip50Ratio·diedRatio 의 분모 = 끝 조건의 분모).

### ▣ 1차 스윕 **끝** (19:01 · 네 팔 다) — 답은 16 아래

| 팔 | 맞은수/초 | hp최저(중앙) | hp<50% 층 | 죽은 층 | 층당 처치 | 완주 | 판정 |
| before(0) | 0.1 | 100% | 0% | 0% | 141 | 228.1s | 미달(안 아픔) |
| m16 | 0.3 | 41% | **60%** ✓ | **30%** (상한 20 넘음) | 81 | 85.8s | 살짝 셈 |
| m24 | 0.7 | 6% | 67% ✓ | **67%** 크게 넘음 | 33 | 54.3s | 너무 셈 |
| m32 | 0.9 | 2% | 100% ✓ | **90%** | 29 | 54.2s | 훨씬 너무 셈 |

**죽은 층이 0 → 30 → 67 → 90% 로 단조 증가.** 예상대로 m32 는 버리는 값이고,
**끝 조건(죽은 층 5~20%)을 만족하는 곳은 16 «아래»** 다. m16 은 hp<50% 조건은 이미 넘었다.
1차 로그 원본: `tmp/necro_hs_v203b_sweep_pass1.log`.

### ▣ 2차 스윕 (브래킷 8·12·14) — **「단조」가 깨졌다 · 자가 창보다 4배 거칠다** (2026-08-31 20:00)

m8·m12 가 들어오자 1차의 「죽은 층이 단조 증가」가 **무너졌다.**

| 팔 | 죽은 층 | hp최저(중앙) | hp<50% 층 | 층 수(분모) |
| before(0) | 0% | 100% | 0% | 15 |
| **m8** | **63%** | 3% | 75% | 8 |
| **m12** | **63%** | 21% | 75% | 8 |
| m16 | **30%** | 41% | 60% | 10 |
| m24 | 67% | 6% | 67% | 9 |
| m32 | 90% | 2% | 100% | 10 |

**곱을 «반으로» 줄인 m8 이 m16 보다 두 배 더 죽는다.** 1차에서 단조로 보인 건
점이 셋뿐이었기 때문이다 — 손잡이가 아픔을 키우는 건 맞지만 **그 자로는 8 과 16 을 못 가른다.**

**왜 — 자가 창보다 4~5배 거칠다.**
· 끝 조건 창은 **죽은 층 5~20%** 로 **15 점 폭**이다(`hs_v203b.mjs:239`).
· 그런데 팔당 층이 **8~10 개**뿐이라 그 비율의 95% 구간이 **±35 점**이다:
  m8 은 5/8 → 참값이 **24%~91%** 어디든 될 수 있다. m16 은 3/10 → **6%~65%**.
· 그래서 **m8 과 m32 조차 통계적으로 구분이 안 된다**(Fisher p=0.28). 곱이 4배 차이인데도.
  유의하게 갈리는 건 m16 vs m32 (p=0.02) 한 쌍뿐이다.
· 15% 와 35% 를 가르려면 **팔당 층 73 개**가 필요하다(지금 8~10 · 20% vs 30% 이면 293 개).

**창 자체는 문제가 아니다** — 도달 가능한 조합 216 중 73 개(33.8%)가 5~20% 안에 든다.
막힌 건 **표본 수**다. 브래킷을 8/12/14 로 좁히는 건 **잡음보다 작은 차이를 재는 일**이라,
m14 가 무슨 수를 뱉든 그 수는 답이 아니다.

**그래서 다음은 셋 중 하나 (감시가 임의로 고르지 말 것):**
① **표본을 7~9배로** — 씨앗 3 → 20+, 팔당 ~2시간. 지금 자 그대로, 답은 나온다. 느리다.
② **자를 연속량으로 바꾼다** — 층당 0/1 「죽었나」 대신 **hp 최저(연속)** 나 **첫 죽음까지의 깊이**로
   판정한다. 한 층이 주는 정보가 훨씬 커서 같은 시간에 훨씬 잘 갈린다. **이쪽을 권한다.**
③ **축을 의심한다** — 팔 여섯을 태웠다([[watchdog-digs-one-axis]]). `FOE_DMG` 곱 하나로
   「가끔 죽는 벽」을 만들 수 없는 것일 수도 있다(죽음이 hp 곡선이 아니라 **몰림**에서 온다면).

2차 로그: `tmp/necro_hs_v203b_sweep.log` · JSON `tmp/hs_v203b_{m8,m12,m14}.json`.


### ▶ 2차 스윕 **도는 중** — 브래킷 **8 · 12 · 14** (2026-08-31 19:30 띄움)
`tools/hs_v203b_sweep.sh` 의 mul 목록을 `8 12 14` 로 고쳐 씨앗 1,2,3 로 띄웠다(PID 82781).
로그 `/Users/lbs/.openclaw/workspace/tmp/necro_hs_v203b_sweep.log` · 마커 `…sweep.log.done`.
1차가 네 팔에 53분 걸렸으니 세 팔은 **대략 20:10 전후** 예상. 유력 후보는 **12**.

**깨어나면(이어받기):**
1. `…sweep.log.done` 이 **없으면** — 도는 중이다. **다시 띄우지 말고** 로그 tail 만 보고 조용히 물러난다.
2. 있으면 `tmp/hs_v203b_{m8,m12,m14}.json` 의 `row.pass` 를 보고 **끝 조건을 만족하는 가장 낮은 mul** 을 고른다.
   (셋 다 미달이면: 죽은 층이 8 에서도 20% 를 넘으면 **4·6** 으로 한 번 더 내려가고,
    14 에서도 hp<50% 가 30% 에 못 미치면 **15** 를 잰다.)
3. 고른 mul 을 `hs/main.js` 의 `FOE_DMG_MUL` 기본값(현재 24)에 박고, **최종 확인**을
   `node tools/hs_v203b.mjs <mul> 5 1,2,3,4,5` + `node tools/hs_v203b.mjs 0 5 1,2,3,4,5`(before) 로
   **씨앗 5개**로 다시 잰다([[seed-the-probe]]). 컷 `tmp/hs_v203b_{ranged,hurt,death}.png` 를 눈으로 본다
   ([[play-it-before-measuring-it]] — 화살이 맞나·hp 바 깎이나·억울하게 죽지 않나).
4. `docs/ROADMAP.md` V-203b 절(before/after 표·정한 값과 근거·컷 경로) + 이 파일 갱신 → **검증후 커밋**.

## ✅ V-204 — 벽·바닥·문틀을 «있는 에셋»으로 : **닫음** (2026-08-31, 커밋 85efdbf 검증전 · 이 커밋 검증후)
병수님 16:43 「맵 픽셀랩 써서 만든 거 맞아? 왜 이렇게 투박하니?」 — 맞다, 벽이 코드 그라디언트였다.
㉠ 벽 = `wall.png` 무늬(오프스크린 한 번 굽고 재활용, 빛은 무늬에 구움) · ㉡ 바닥 = 층 깊이로 갈아 낌
(crypt→bone→rot→…, B1 회색·B3 갈색·B5 붉은결) · ㉢ 문설주 = `pillar.png`. 자 `tools/hs_v204_paint.mjs`.
**코드 도형 비율(B1, 오버레이 제외) 56.2%→54.9%** · terrain p95 **0.2ms** · 벽밖 0% · 콘솔오류 0.
큰 코드 넓이는 벽이 아니라 «공허·비네트·물들이기»(의도된 연출)였다는 게 자로 드러났다. 상세·컷은 `docs/ROADMAP.md` V-204.

## ▶ 다음 차례 — **V-203b 마무리(위 이어받기), 그 다음 V-205**
· **V-203b** — 스윕 `.done` 을 보고 mul 을 골라 박고 씨앗 5개로 최종 확인 → 검증후 커밋(위 블록의 이어받기 4단계).
· **V-205 감(로드맵에 적어 둠)** — [[carry-fixes-forward]] 훑기서 남은 코드 도형: 적 발사체 foeShots(주황 fillRect 막대) · 금 알갱이 golds(arc 점) 를 에셋으로.

## ✅ V-201 — 지형지물 무시 이동 : **닫음** (2026-08-31 12:40, 커밋 578c5e3/45b2f70)
걸을 수 있는 자리 = 방 ∪ 복도. 벽밖 **63.58% → 0%** · 소품겹침 1.07% → 0%.

## ✅ V-202 — 맵 구조를 BSP 로 : **닫음** (2026-08-31 12:56, 커밋 da85f49)
관통복도 1.63→**0** · 바닥비율 40.2→48.4% · 방개수 8→25(깊이로 늚) · 도달률 100%.

## ★★ 멈추지 말 것 — 2026-08-31 14:04~16:43 에 **2시간 40분을 통째로 쉬었다**
14:04 에 이 파일에 「V-203 은 병수님 답을 기다린다(감시가 임의로 띄우지 않게)」라고
적었더니 **감시가 그 뒤로 아무것도 안 했다.** 병수님이 16:43 에 「한참동안 진행상황
보고가 없네? 뭐하고 있니?」라고 물어서야 알았다.
· **물음을 던지는 것은 옳았다**(어느 쪽으로 아프게 할지는 취향이다). 틀린 것은
  **「답이 올 때까지 아무것도 하지 말라」고 못박은 것**이다.
· **규칙: 병수님 결정을 기다리는 항목이 있어도 감시는 «다른 열린 항목»을 집는다.**
  기다리는 항목은 「대기」로 표시하되, **할 일 목록을 비우지 않는다.**
· 그리고 취향이 갈리는 자리는 **답을 기다리는 대신 «세 팔을 다 재서 표로» 내놓는다** —
  고르는 사람이 수를 보고 고르면 되고, 그 사이 일은 굴러간다.

## ✅ V-202b — 밸런스에 천장 : **닫음** (2026-08-31 14:10)
자 `tools/hs_v202b_shape.mjs`(씨앗 5 × 층 1~5 · 층당 45s 자동조작)로 **처음으로**
「한 판이 어떤 모양인가」를 쟀다. `SLOT_CAP_MAX=24` · `BUILD_SLOTS_CAP=16` ·
`MINION_MUL_CAP=4` + 방당 팩 3~4→2~3 · 팩당 14~24→10~14.
층당 놓인 적 330~620 → **199~377** · 쓴 자리 42~54 → **24** ·
소환수 한 방 피해 **405K~1.26M → 3.3K~7.6K**(곱 ×705 → ×4) · 오류 0.

## ✅ V-203 — **긴장의 세 팔을 재서 표로** : **잼 끝 · 대기(병수님 선택)** (2026-08-31 17:26 · 커밋 a60d1f8)
팔 셋(`__ENEMY_REACH`/`__RANGED_MOB`/`__CHARGER_MOB`)을 전부 globalThis 손잡이(**기본 꺼짐**)로 넣고,
자 `tools/hs_v203_arms.mjs` 로 각각 켜서 쟀다. 하나를 골라 넣지 않았다 — 고르는 건 병수님이다.

| 팔 | 맞은수/초 | hp최저 | hp<50% 층 | 죽은 층 | 층당 처치 | 완주 |
| 지금(off) | 0 | 100% | 0% | 0% | 132 | 137s |
| ENEMY_REACH | 0.2 | 97% | 0% | 0% | 125 | 137s |
| RANGED_MOB | 0.8 | 96% | 0% | 0% | 135 | 137s |
| CHARGER_MOB | 0.1 | 100% | 0% | 0% | 130 | 137s |

- **벽 넘기 세기: RANGED(0.8) > REACH(0.2) > CHARGER(0.1)** — 원거리가 벽 위로 쏴 제일 많이 닿는다(화살이 소환수를 무시).
- **그런데 어느 팔도 안 아프다** — 적 피해가 사람 hp(4,515)에 견줘 너무 작다. 「벽 뒤 안전」의 절반은 소환수가 아니라
  **«피해가 없다»**는 것이었다. 긴장은 **RANGED(닿게) + 적 피해↑(아프게)** 두 손잡이로 온다(내 생각 · 판정 아님).
- 상세 표·컷·근거는 `docs/ROADMAP.md` V-203 절. 컷 `tmp/hs_v203_{reach,ranged,charger}.png` · JSON `tmp/hs_v203_*.json`.

**깨어나면:** `git status` 를 본다(V-203 은 두 번 커밋됨 — 검증 전 a60d1f8 · 검증 후). V-203 은 **잼 끝 · 병수님 답 대기**다.
⚠️ **감시가 임의로 팔을 켜지 말 것** — 기본 꺼짐이 곧 지금 판이다(off 행이 V-202b 와 byte-동일). 답을 기다리는
«동안 다른 열린 항목을 집는다» — 다음은 **V-204**(맵을 픽셀랩 에셋으로: `wall.png`·15종 바닥이 논다). ROADMAP 맨 아래 V-204 절이 지시문이다.

### ▣▣ V-203b **진짜 원인 — 손잡이가 「죽느냐」를 안 정한다** (2026-08-31 20:05 · 브라우저 안 씀)

②(연속량 자)를 권하기 전에, **이미 모아 둔 JSON 으로 먼저 시험했다**(층별 `hpMin` 이 다 들어 있다).
결과는 자를 바꿔도 안 낫는다 — 그리고 **왜 안 낫는지가 답이었다.**

| 팔 | hp최저 낮음(<20) | 중간(20~80) | 높음(>80) | 평균 ± 95% |
| before(0) | 0 | 1 | 14 | 98.5 ± 3 |
| m8 | 5 | 1 | 2 | 26.9 ± 27 |
| m12 | 4 | 3 | 1 | 28.6 ± 24 |
| m16 | 4 | 3 | 3 | 45.4 ± 23 |
| m24 | 6 | 3 | 0 | 20.1 ± 18 |
| m32 | 9 | 1 | 0 | 3.6 ± 6 |

**아픈 팔 다섯에서 층의 76% 가 «거의 죽거나(hp<20)» 아니면 «거의 안 맞거나(hp>80)» 다.**
중간대는 24% 뿐 — hp최저는 연속량처럼 안 굴러가고 **쌍봉**이다.

**그래서 손잡이를 아무리 돌려도 「가끔 죽는 벽」이 안 나온다.**
`FOE_DMG` 곱은 **몰렸을 때 얼마나 아픈가**만 정한다. 그런데 죽고 사는 걸 가르는 건
**애초에 몰렸느냐** — 그건 층마다 동전 던지기고, 그 동전이 분산을 다 먹는다.
연속량 자(평균±95%)로도 끝은 갈리지만(before · m32) **브래킷 8~24 는 여전히 서로 겹친다** —
자를 바꿔서 될 일이 아니라는 뜻이다.

**판정: ③ 축이 틀렸다.** 곱을 8·12·14 로 좁히는 2차 스윕은 **답이 없는 자리를 파고 있다.**
「가끔 죽는다」를 만들려면 **몰림 자체**를 손잡이로 잡아야 한다 — 한 번에 붙는 적 수 상한,
소환수 벽이 뚫리는 조건, 방 진입 시 스폰이 터지는 방식. 피해 곱은 그 위에 얹는 **2차 손잡이**다.
(→ 곱은 m16 근처에 두고 고정 · 다음 항목은 **몰림 축**으로 연다. [[watchdog-digs-one-axis]] 여섯 팔째)

2차 스윕 m14 는 20:12 께 끝난다 — **그 수는 참고만 하고 브래킷은 더 안 좁힌다.**

### ▣ **판정 (2026-08-31 20:40) — ①②는 접는다, 원천을 고친다 → V-205**

2차 스윕 뒤 「셋 중 하나」로 남겨 뒀던 갈림길을, **새로 돌리지 않고 이미 있는 JSON 을
다시 읽어** 정리했다(공짜 · 2시간 아낌).

**② 연속 자는 안 통한다 — hpMin 이 쌍봉이다.** 층별 hpMin 분포:

| 팔 | 0-10 | 10-30 | 30-60 | 60-90 | 90-100 | 맞은수/층 |
| before | 0 | 0 | 0 | 1 | 14 | 5 |
| m8 | **5** | 0 | 1 | 1 | 1 | **37** |
| m12 | 4 | 2 | 0 | 1 | 1 | 21 |
| m14 | 3 | 2 | 1 | 0 | 2 | 23 |
| m16 | 1 | 4 | 1 | 2 | 2 | **9** |
| m24 | **6** | 0 | 1 | 2 | 0 | 12 |
| m32 | **8** | 1 | 1 | 0 | 0 | 15 |

값이 0 근처 아니면 100 근처다 — 「죽었나」가 연속량 옷을 입은 것뿐이다.
실제로 hpMin 평균으로 다시 갈라 봐도 **m8 vs m16 은 t=-0.96 로 여전히 못 가른다**
(갈리는 건 before(t=+4.21)·m32(t=-3.22) 뿐 — 1차 자와 똑같은 한계).

**③ 이 맞다 — 축이 틀렸고, 그 까닭까지 나왔다.**
`맞은수/층` 이 **손잡이와 무관하게 4배 흔들린다**(m8 37 · m16 9 · m24 12 · m32 15).
곱은 «맞을 때의 피해»만 바꾸지 «몇 대 맞는지»는 못 바꾼다. 그런데 4배가 흔들리니
**죽음을 정하는 건 손잡이가 아니라 이 흔들림**이다 — m8 이 m16 보다 두 배 더 죽는
역전이 여기서 나왔다.

**흔들림의 원천: 벽시계 dt.** `hs/main.js:2265`
`const dt = Math.min(0.05,(now-last)/1000)` — `performance.now()` 를 그대로 쓴다.
헤드리스 크롬 프레임 간격이 판마다 달라 **씨앗을 고정해도 판이 갈린다**
([[same-seed-is-not-same-run]]). ① 표본 7~9배는 이 잡음을 «견디려는» 시도라 접는다.

## ▶ V-205 — 자의 잡음을 뿌리에서 없앤다 : **㉠㉡㉢ 커밋됨 · ㉣ 스윕 도는 중** (2026-08-31 20:33 띄움, 21:00 확인)
잡 살아 있음(pid 93674, 27분째 / 상한 90분). 로그 `/Users/lbs/.openclaw/workspace/tmp/necro_hs_v205.log`,
끝나면 `…log.done`.

**닫힌 것(커밋 8bd2a43·f2b0273·0af3ade):**
㉠ `__FIXED_DT>0` 이면 dt 고정 · 연출 sin 은 누적 게임시간 · `__gameSec` 노출 (평소 플레이 byte-동일)
㉡ `tools/hs_v203b.mjs` 의 층 판정·상한·`sec` 을 게임시간 기준으로, 벽시계는 3배 안전장치로만
㉢ **결정성 합격** — 층1 씨앗1 을 두 번 돌려 kills 137 · hitN 20 · hpMin 23 **완전 일치**.
   (봇 AUTO 재발동 cadence 도 `performance.now` → `__gameSec` 로 바꿔야 맞았다.)

**남은 것 ㉣:** 씨앗 1,2,3 · 곱 8·16·24 재측정 → **m8 > m16 역전이 사라지는지**.
사라지면 브래킷 좁혀 끝 조건 조준. **역전이 그대로면 손잡이 축이 틀린 것** → 억지로 고르지 말고
그 판정을 여기 적고 멈춘다.


## ⛔ V-205 한계선 — **22:05 까지 결정성이 안 나오면 이 축을 접는다** (2026-08-31 21:30 감시가 정함)

21:30 현재 V-205 잡은 57분째 돌고 있고(상한 90분 → 22:03 경), **㉢ 결정성은 아직 불합격**이다.
로그에 `→ 씨앗 1: 비결정 (오류 0)` 이 찍혔고, 지금은 층 기록을 층번호가 아니라 **차례(index)로**
비교하도록 자를 고쳐 다시 재는 중이다. 21:00 커밋의 「㉢ 합격」은 **층 1 스모크 한 판**이었고,
층 1→2 전체로 넓히니 깨졌다.

**여기서 셈을 한 번 해야 한다.** 18:00 부터 3시간 반 동안 판 것이 전부 **같은 축**이다 —
V-203b(스윕) → V-203b 2차 → V-205(측정 자 고치기). 마지막 한 시간은 **게임이 아니라 자**를 만들고 있다.
병수님이 만지는 것은 하나도 안 바뀌었다.

- **22:05 감시가 판정한다.** `tmp/necro_hs_v205.log[.done]` 을 보고:
  · **결정성 합격 + ㉣ 스윕까지 나왔다** → 브래킷을 좁혀 끝낸다(이 축을 마저 판다).
  · **아직 비결정이거나 잡이 시간상한에 잘렸다** → **이 축을 여기서 접는다.** 남은 비결정 원천을
    `docs/ROADMAP.md` 에 적어 두고(다음에 필요하면 이어받게), **손으로 켜서 30초 해 보는 쪽**으로
    돌아간다 — 병수님이 느끼는 「치는 맛」이 밸런스 곱보다 앞선다([[play-it-before-measuring-it]]).
- 접기로 판정하면 **곱은 m16 으로 두고 넘어간다**(1차·2차에서 유일하게 죽은 층 30% 로 20% 문턱에
  제일 가까웠던 팔). 완벽한 값을 못 골랐다는 사실을 적고 넘어가지, 값을 지어내지 않는다.

## ⛔⛔ V-205 판정 — **접었다 (2026-08-31 22:05, 예고대로)**

22:05 한계선에서 본 것: **비결정 잔존 + 잡이 90분 상한에 잘림**(pid 93674, 93분째 로그 10분 멈춤 → 정리).
두 조건이 다 걸려서 21:30 에 미리 적어 둔 대로 **이 축을 접는다.**

- **어디까지 갔나:** 봇틱은 결정적이 됐다(1445=1445 · 1448=1448 — 입력은 같아졌다).
  그런데 **게임프레임 수가 1507 vs 1511 로 갈리고**, 따라서 RNG 호출수가 27584 vs 28627 로 갈린다.
  한 번은 완전 일치가 나왔지만 세 번 더 돌리니 두 번 깨졌다 — **간헐적**이다.
- **남은 원천(적어 뒀다, `docs/ROADMAP.md` V-205):** 자가 `__gameSec` 를 **밖에서 폴링**해 끝내서
  폴링 간격 안에 몇 프레임이 더 돈다. 고치려면 **페이지 «안»에서 목표 게임시간 프레임에 스스로 멈추게** 해야 한다.
  (`Math.random` 19곳 · `Date.now` 0곳 — 원천은 RNG 가 아니라 프레임 수다.)
- **곱은 `FOE_DMG_MUL = 16` 으로 확정하고 넘어간다**(커밋 471bba5). 자가 8 과 16 을 못 가르는 상태에서
  고를 수 있는 최선이고, **완벽한 값이 아니라는 사실을 적어 두고** 넘어간다.

### ▶ 다음: **손맛** — 자 말고 켜서 본다
22:07 컷(`tmp/hs_v201_fight.png`, m16 적용 후)을 직접 열어 보고 잡은 것 둘:
1. **m16 은 실제로 아프다** — 교전 한 판에 hp 337/3315(10%)까지 내려갔다. 「안 아픔」 문제는 닫혔다.
2. **화면 아래 절반이 빈 공허다.** 사람이 방 남쪽 벽에 붙으면 카메라가 방 밖 검은 데를 크게 비춘다.
   그리고 **빽창이 벽을 넘어 공허로 날아간다**(컷 오른쪽 아래 청록 창 넷). 둘 다 「치는 맛」을 깎는다.
   → 다음 판은 이 둘 (㉠ 카메라를 방 안으로 물리기 · ㉡ 발사체도 벽에 막히기).

## ▶ V-206 — 손맛 둘 (카메라 공허 · 발사체 벽 통과) : **띄웠다** (2026-08-31 22:35, PID 18036)
지시문 `tmp/hs_v206_prompt.md` · 로그 `/Users/lbs/.openclaw/workspace/tmp/necro_hs_v206.log`
마커 `…log.done`. 상한 85분(≈ 24:00).

**한 일 없음(아직) — 이건 착수 기록이다.** 22:05~22:35 사이에는 **아무것도 안 돌고 있었다**
(V-205 를 접은 뒤 다음 잡을 아무도 안 띄웠다 — 30분 공백). 감시가 그걸 잡아 띄웠다.

**끝 조건:** ㉠ 화면 공허 비율 p95 가 before 의 절반 이하 · ㉡ 벽 밖 발사체 **0%** · 콘솔오류 0
· 컷 `tmp/hs_v206_{corner,shot,room}.png` 를 눈으로 봄.

**깨어나면:** `…log.done` 없으면 로그 tail 만 보고 **다시 띄우지 말 것**. 있으면 커밋 여부 ·
공허/벽밖 수 · 컷 세 장을 직접 열어 보고 판정 → 병수님께 보고.

## ✅ V-206 판정 — **㉡ 통과 · ㉠ 미달(그러나 병수님이 본 증상은 고쳐졌다)** (2026-09-01 00:05)

잡은 5100초 상한에 잘렸다(exit 124) — **검증은 다 끝났는데 커밋을 못 하고 죽었다.**
감시가 이어받아 `hs/main.js`(카메라 clamp 를 `playerRegion` → `localBounds` 로 다시 씀)를 커밋했다.
측정(23:36)·컷(23:55) 모두 이 트리(23:15 main.js)의 것이므로 수치와 코드가 일치한다.

- **㉡ 벽 밖 발사체 41.9%(72012/171759) → 0%(0/47170)** ★ 통과. 콘솔오류 0.
- **㉠ 공허 p95 «절반 이하»: 미달** — 전체 67.1%→68.4%, 아래공허 80.7%→77.1%.
- **그런데 손잡이 토글 컷(같은 프레임, `__CAM_CLAMP` 만 뒤집음)에서는 증상이 분명히 사라진다** —
  `cam_before` 는 화면 아래 ~25% 가 검은 암반, `cam_after` 는 남쪽 벽이 화면 밑변에 붙어 방이 화면을 채운다.

**자가 틀렸던 것:** 공허 p95 를 **모든 프레임**에 걸쳐 쟀는데, 놀이 시간 대부분은 **화면보다 작은 방**
안이고 거기선 공허가 맵+고정줌의 성질이라 카메라를 어디에 둬도 안 줄어든다(스펙이 「작은 방은 가운데,
줌 안 바꿈」을 골랐다). 병수님이 말한 건 **큰 방에서 벽에 붙었을 때**뿐인데 그 순간은 전체 프레임의
일부라, 「절반」 문턱은 애초에 이 자로는 못 넘는다 — [[threshold-and-ruler-must-match]] 를 또 밟았다.
다음에 이 축을 다시 재려면 **벽에 붙은 프레임만 골라** 재야 한다.

## ✅ V-207 — 「뭔가 잘못됐다」: 걸어서 보고 고친다 : **닫음** (2026-09-01 01:15, 커밋 a6cee7e 검증 후)
걸어서(순간이동 없이) 재니 1차 커밋(`9787890` WAKE 820)은 **아직 미달**이었다 — 동시 무리 p95 3·적 32,
7.2s 만에 죽음. **WAKE 500 으로 더 내려** 동시 무리 p95 **2 ✓**·적 22·62s 생존·처치 22 로 잡았다.
방당 팩 2~3·팩당 10~14 고정(V-202b)이라 820 은 방 전체+옆방을 덮어 3 팩이 함께 깨어난 게 원인.
500 ≈ 화면 반폭 → 「이 방의 무리」만, 화면에 들어올 때 깨어난다(V-145 복원).
- **자:** `tools/hs_v207_walk.mjs`(p6_run 걷기·감지 재사용 · 손잡이 `globalThis.__WAKE`). JSON `tmp/hs_v207_walk.json`.
- **컷(WAKE 500) 석 장 눈으로 판정 → 「던전으로 보인다」:** `tmp/hs_v207_walk{1,2,3}.png` — 사람이 또렷이
  서고, 이 방의 무리(팩 2)와 붙고, 소환 8/8 로 루프가 돈다. 붉은 고리 그물 사라짐.
- **눈에 걸린 것 하나(C):** 잡몹 발밑 붉은 고리(α0.9·2.5px)가 팩으로 뭉쳐 «붉은 그물» → 잡몹만 얇고 옅게
  (1.6px·α0.4), 정예는 굵게. 상세·앞뒤 표는 `docs/ROADMAP.md` V-207 절.
- **회귀:** 벽밖 0% · 발사체 벽밖 게임 강제(코드 안 건드림) · 콘솔오류 0 · fp95 41.7ms(예산 안).

## ▶ 다음 차례 — **① V-210(잠든 무리 포갬, 눈에 제일 크게 걸림) · ② V-208 검증 · ③ 밸런스**
① **V-208(`db095bc`, 검증 전)** — 몸 배율 1.5→1.15·모든 몸 ×0.72. V-207 컷이 V-208 적용 트리에서 나왔고
   사람이 판을 덜 먹어(읽힘 좋아짐) 눈으로는 무리 없어 보였다. 다만 **전용 자로 「사람이 판의 몇 %」를 앞뒤로**
   재서 닫아야 한다(아직 안 함).
② **밸런스 — 「팩 하나를 못 비운다」.** V-207 에서 봇이 팩 둘을 깨우고도 62s 안에 한 방을 완전히 비우진
   못했다. WAKE 는 이제 맞으니 다음 벽은 밸런스다 — 소환 없이 시작하는 네크로맨서가 첫 팩을 얼마나 버거워하나.
   자 말고 **손으로 켜서 30초** 해 보는 쪽부터([[play-it-before-measuring-it]]).
