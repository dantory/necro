# 지금 도는 일 (감시가 제일 먼저 읽는 파일)

감시 크론의 지시문은 **낡을 수 있다.** 이 파일이 최신이다.

## ✅ V-201 — 지형지물 무시 이동 : **닫음** (2026-08-31 12:40, 커밋 578c5e3)
병수님 「맵의 지형지물을 다 무시하고 이동된다」(08-31 11:35).
걸을 수 있는 자리 = 방 ∪ 복도. stepTo 축분리 미끄러짐 + unstick.
자 `tools/hs_v201_walk.mjs` — 벽밖 **63.58% → 0%** · 소품겹침 1.07% → 0% ·
끼임창 0(최대정지 5f) · compute p95 2.6ms. 컷 넷 눈으로 확인 · 병수님께 보고 완료.

## ▶ V-202 — 맵 구조를 BSP 로 (돌고 있음)
병수님 「맵도 좀 이상하고」(08-31 11:36). `genFloor` 가 방을 무작위로 뿌리고
복도를 방중심→방중심 L 자로 이어 **남의 방을 관통**한다. 방 크기도 다 비슷하다.

- 지시문: `/Users/lbs/.openclaw/workspace/tmp/hs_v202_prompt.md`
- 로그: `/Users/lbs/.openclaw/workspace/tmp/necro_hs_v202.log`
- 마커: 같은 경로 `.done` (내용이 exit code · **124 면 시간초과**)
- 띄운 때: 2026-08-31 12:40 · timeout 3000s (≈50분)

**깨어나면:** 마커를 본다.
- 있고 rc=0 → `git status` 로 **커밋됐는지** 확인(안 됐으면 검증하고 커밋),
  `tmp/hs_v202_map_*.png`·`hs_v202_ingame*.png` 를 **직접 열어** 구조가 읽히는지 보고,
  before/after 수치와 함께 병수님께 보고.
- 있고 rc≠0(특히 124) → 작업 트리에 남은 변경을 **직접 검증·마무리·커밋**한다.
  V-201 도 rc=124 로 끝났고 코드는 멀쩡했다 — 마커만 보고 버리지 말 것.
- 없으면 → 상태 한 줄만 적고 끝낸다(노이즈 금지).

## 다음 (V-202 뒤)
**V-202b — 밸런스.** 소환 자리가 8 → 98 까지 불고 한 층에서 2000 마리를 죽인다.
「한 판이 어떤 모양인가」를 재는 자가 아예 없다. 그림만 보는 자뿐이었다.
