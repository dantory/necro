# NOW — hs/ 감시가 «제일 먼저» 읽는 파일
(크론 본문의 항목번호보다 이게 우선이다. 축을 띄울 때마다 여기를 갱신한다.)

## 지금 도는 축 — V-229 「물러섬을 «떨림» 아니라 «결정»으로」
STARTED=2026-09-01 23:05 · PID 26549 · ETA ~23:25
  로그 `~/.openclaw/workspace/tmp/necro_hs_v229_hold.log[.done]` · per-cell `tmp/hs_v229_hold.json`
  러너 `tools/hs_v229_hold.sh` (V229_ARMS=1 · RET_HOLD_A=1.2) · 되돌림 `RET_HOLD=0`
  두 팔 = BEFORE 유지 0s(V-228 과 **같은 코드 경로**) / AFTER 유지 1.2s — 문턱·게임 손잡이는 전부 고정

  왜: V-228 이 갈림을 끝냈다 — 게임을 하나도 안 건드리고 자에 물러섬만 주니
      죽음 53.3% → **21.4%** · 밴드 33.3% → **71.4%** · hp최저 중앙 3% → **19%**.
      「죽음 66.7%」는 게임이 치명적이라서가 아니라 **자가 안 물러서서** 난 수였다.
      **그런데 그 자는 커버리지를 무너뜨렸다** — 면적 17.3→7.9% · 방문 55.6→23.1% · 한 층 굶음(처치 0).
      층당 물러섬 **428회/31.6s = 74ms 마다 뒤집힘**. 복귀선 70% 는 재생 0.48%/s 로 층 시간 안에
      도달 불가라 실질 탈출구가 `!foes` 뿐 → 한 발 물러서면 반경(460px) 밖 → 즉시 복귀 → 재진입.
      무리 «가장자리에서 떤» 것이다. 면적 7.9% 짜리 자로는 눈금을 다시 박을 수 없다.

  **갈림:**
  · AFTER 커버리지가 뚜렷이 오르고(면적 ≥12% 목표) 죽음 ≤20% 유지 → 이 자를 기본으로 박고
    **밴드/죽음 눈금을 이 자로 다시 박는다(V-230)**.
  · 커버리지가 안 오르면 → 진동이 원인이 아니다. 도달 불가한 복귀선 `RET_OUT` 을 만진다.
  회귀 = 완주 137~319s · 오류 0 · 몸벽밖 0% · **굶은 층 0**.

## 다음 감시가 할 일 (순서대로)
1. `necro_hs_v229_hold.log.done` 이 있으면 → 두 팔의 **커버리지(면적/방문) · 죽음 비율 · 굶은 층 · 물러섬 횟수**를 본다.
   위 «갈림» 대로 판정하고 V-229 를 닫는다(ROADMAP·NOW 둘 다).
2. `.done` 이 없으면 → PID 26549 살았는지 보고 상태 한 줄. **다시 띄우지 말 것.**
3. 그 뒤: 갈림대로 V-230(눈금 재박기) 또는 RET_OUT 축. 대기: V-224(발사체 벽밖 2.1~12.2%).

## 닫힌 것 — 다시 집지 말 것
- **V-228** 닫음 (2026-09-01 22:32). **게임 탓이 아니라 «자» 탓이었다.** 게임 손잡이 전부 고정,
  물러섬만 달았더니 죽음 53.3→21.4% · 밴드 33.3→71.4%. ⇒ **곱(16)·적 dmg 는 아직 만지지 않는다.**
  남은 흠은 커버리지 붕괴 하나이고 그게 V-229 다. 되돌림 `BOT_RETREAT=0`.
- **V-227** 닫음 = **기각**. 「길 위 남은 거리」 진척은 모든 깊은 층에서 더 나빴다. 되돌림 `NAV_PATHPROG=1`.
  ★ 축을 띄운 근거가 한 판짜리 표본이었다 — 깊은 층 커버리지는 **씨앗 편차가 증상보다 크다.**
- **V-226** 닫음 (`c64a282`). 깊이 곡선을 떼니 층5 압박비 5.97 → 1.01.
- **V-225** 닫음. 간선 규칙은 고쳤고 기본으로 남겼지만 게임 안에서는 경로없음 0 이라 원인이 아니었다. 되돌림 `NAV_LEGACY=1`.

## 지금 믿는 눈금 (물러서는 자 · 씨앗 1/2/3 × 층 1→5 · 2026-09-01 22:32 · V-228 AFTER)
  교전층 14/15(굶음 1) · 밴드 **71.4%** [≥25% ✓] · 죽음 **21.4%** [5~20% · 한 판 차이]
  hp최저 중앙 19% · 완주 271.2s [✓] · 커버리지 면적 **7.9%** / 방문 **23.1%** ◀ **이 자의 흠**
  회귀 벽밖 0% · 발사체벽밖 2.9% · 오류 0 · frame p95 50.1ms
  ★ 커버리지가 이 값인 동안에는 **이 눈금으로 새 축을 판정하지 말 것**([[ruler-coverage-is-a-number]]).

NOTE=병수님 07:23 「긴장은 니가 알아서 만들어라 왜 자꾸 나한테 정하래」 — 값은 재서 정하고
  되돌릴 손잡이만 남긴다. 「선택 대기」 금지.
