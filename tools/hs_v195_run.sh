#!/bin/bash
# V-195 — 축을 «보이는 것»으로 옮긴다. 화면 아래(떠오르는 글자·도움말·소수점)를 고친다.
. "$(dirname "$0")/ab_guard.sh"
cd "$(dirname "$0")/.." || exit 1

python3 /Users/lbs/.openclaw/workspace/tools/opencode_safe_run.py \
  --timeout 2400 --cwd "$PWD" "$(cat <<'PROMPT'
⛔ 최우선 규칙 — 이걸 어기면 실패다
이 작업은 **무인 배치**로 돈다. 사람이 없고, 네가 턴을 끝내면 **아무도 이어받지 않고 그대로 종료된다.**
1. **서브에이전트/탐색 에이전트를 띄우고 기다리지 마라.** 필요한 파일은 네가 직접 Read/Grep 해라.
   `hs/main.js` 는 길다 — offset/limit 으로 나눠 읽어라.
2. **턴을 중간에 끝내지 마라.** 「결과가 오면 이어가겠다」·「여기서 마칩니다」·「에이전트를 기다리겠다」 전부 금지.
3. **승인·확인 질문 금지.** 답할 사람이 없다. 애매하면 **회귀 위험이 가장 적은 해석**을 골라
   한 줄로 로그에 남기고 계속 가라.
4. **계획만 있는 응답은 실패다.** 최종 응답에 반드시: 고친 파일 · 검증 수치 · 커밋 해시.
5. **막히면 줄여서라도 완주해라.** 결함 넷 중 둘만 고쳐도 **동작 + 검증 + 커밋**에 도달한 쪽이 정답이다.
6. 예산 36분. 탐색에 6분 넘게 쓰지 마라.

★★ 긴 측정은 «한 번에 하나씩, 백그라운드로» 돌려라 — 앞선 판이 시간 상한(rc=124)에 잘린 적이 있다.
   `nohup node tools/xxx.mjs ... > tmp/_log 2>&1 &` 로 띄우고 짧은 sleep 으로 나눠 폴링해라.
   **한 호출 안에서 6분 넘게 기다리지 마라.**

★★ 커밋 순서: ① 자를 세우고 **고치기 전 값**을 먼저 잰다 → 그 수를 ROADMAP 에 적고 **커밋**
   ② 고친다 → 다시 잰다 → **커밋**. 작업 트리에만 남은 것은 다음 세션에게 **없는 일**이다.

---

너는 necro 리포의 hs/ (직접 조작 핵앤슬래시 프로토타입, 바닐라 JS + canvas)에서 일한다.
기준 문서: `docs/HS_STYLE.md` · `docs/ROADMAP.md`(**맨 아래 「V-195 착수」 블록이 이 판이다 — 먼저 읽어라**).

## 앞선 판이 남긴 것 (다시 파지 마라)
밀도/배치 축은 **닫혔다**: 교전중 적 p50 36(≥12) · 이름표 6 · 처치당 알갱이 5 · 격자 아님(확대해 확인).
V-193 의 CV 자는 **폐기**다(`tools/hs_v193_shape.mjs` 머리 주석 참고). 그 축을 다시 건드리지 마라.

## 이 판이 할 일 — 화면 아래를 고친다. 늘리지 마라.

### ① 자를 세운다: `tools/hs_v195_hud.mjs`
`tools/hs_v192_dense.mjs` 를 본으로 복사해 고쳐라(CDP 9333 · `node tools/chrome_guard.mjs` 를 먼저 부른다).
자동조종 실플레이, 씨앗 1·2, 각 120초, 1초마다 표본. **게임 안 좌표로** 재라(그림 픽셀 짐작 금지) —
떠오르는 글자 배열(`flyText`/floaters 계열)의 각 원소에서 그린 사각을 계산해 낸다.
  ㉠ **잘린 글자 수** — 사각이 캔버스 밖으로 나가는 글자의 총 개수.
  ㉡ **하단 UI 겹침** — 글자 사각이 스킬바+도움말 줄의 합집합 사각과 겹치는 표본 비율(%).
  ㉢ **글자끼리 겹침** — 동시에 떠 있는 글자 쌍 중 사각이 겹치는 쌍의 비율(%).
  ㉣ **소수점 노출** — 장비/스탯 줄에 소수점 딸린 숫자가 있는지(0/1).
  회귀: 교전중 적 p50(적≥1 표본의 중앙값) · frame p95 · 콘솔 오류 수.
결과는 `tmp/v195_hud.json`, 컷은 `tmp/v195_t{20,60,120}.png`(씨앗 1).

### ② 고치기 전 값을 잰다 → ROADMAP 에 적고 커밋한다
**바닥이 문턱에서 먼지 확인해라.** 어떤 눈금이 이미 0/100 에 붙어 있으면 그건 눈금이 아니다 —
그 사실을 적고 통과선에서 빼라(억지로 맞추지 마라).

### ③ 고친다 (hs/ 게임 코드)
· 떠오르는 글자를 그릴 때 **캔버스 안으로 밀어 넣는다**(clamp: 좌우·상하 모두, 여백 4px).
· 하단 UI 예약 띠를 **한 곳에서 정의**하고, 글자가 그 띠에 들어오면 위로 밀어낸다. 매직넘버를
  흩뿌리지 말고 상수 하나로 두어라.
· 같은 시각에 뜬 글자끼리 **세로로 어긋나게** 쌓아라(간단한 밀어내기면 충분하다. 물리 시뮬 금지).
· 큰 수는 **정수 축약**으로 찍어라(예 ×2.4M). 이미 축약 함수가 있으면 그걸 쓰고, 없으면 하나 만들어
  장비/스탯/피해 줄이 **모두 같은 함수**를 지나게 해라 — 한 곳만 고치면 [[carry-fixes-forward]] 위반이다.
· `ctx.filter` 로 칠하거나 `fillRect` 로 에셋을 흉내내지 마라(에셋은 픽셀랩 파이프라인 전용).

### ④ 다시 잰다 → 회귀도 같이 본다 → ROADMAP 에 «전/후»를 나란히 적고 커밋·푸시
회귀 자도 돌려라: `node tools/hs_v186_tree.mjs 30 7` 와 `node tools/hs_v190_xp.mjs`(있으면).
`node --check` 로 고친 파일 문법 확인. 콘솔 오류 0 이어야 한다.

## 마지막 응답에 반드시 담을 것
고친 파일 목록 · ㉠~㉣ **전/후 수치** · 회귀 결과 · 커밋 해시(둘 이상) · 남긴 컷 경로.
PROMPT
)"
