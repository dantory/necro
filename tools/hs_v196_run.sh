#!/bin/bash
# V-196 — 머리 위 체력바가 몸에서 떠 있다. 바 겹침·무리 단조로움까지 «보이는 것»을 고친다.
. "$(dirname "$0")/ab_guard.sh"
cd "$(dirname "$0")/.." || exit 1

python3 /Users/lbs/.openclaw/workspace/tools/opencode_safe_run.py \
  --timeout 2400 --cwd "$PWD" "$(cat <<'PROMPT'
⛔ 최우선 규칙 — 이걸 어기면 실패다
이 작업은 **무인 배치**로 돈다. 사람이 없고, 네가 턴을 끝내면 **아무도 이어받지 않고 그대로 종료된다.**
1. **서브에이전트/탐색 에이전트를 띄우고 기다리지 마라.** 필요한 파일은 네가 직접 Read/Grep 해라.
   `hs/main.js` 는 길다 — offset/limit 으로 나눠 읽어라.
2. **턴을 중간에 끝내지 마라.** 「결과가 오면 이어가겠다」·「여기서 마칩니다」·「에이전트를 기다리겠다」 전부 금지.
3. **승인·확인 질문 금지.** 답할 사람이 없다. 애매하면 **회귀 위험이 가장 적은 해석**을 골라
   한 줄로 로그에 남기고 계속 가라.
4. **계획만 있는 응답은 실패다.** 최종 응답에 반드시: 고친 파일 · 검증 수치 · 커밋 해시.
5. **막히면 줄여서라도 완주해라.** 셋 중 둘만 고쳐도 **동작 + 검증 + 커밋**에 도달한 쪽이 정답이다.
6. 예산 36분. 탐색에 6분 넘게 쓰지 마라.

★★ 긴 측정은 «한 번에 하나씩, 백그라운드로» 돌려라 — 앞선 판이 시간 상한(rc=124)에 잘린 적이 있다.
   `nohup node tools/xxx.mjs ... > tmp/_log 2>&1 &` 로 띄우고 짧은 sleep 으로 나눠 폴링해라.
   **한 호출 안에서 6분 넘게 기다리지 마라.**

★★ 커밋 순서: ① 자를 세우고 **고치기 전 값**을 먼저 잰다 → 그 수를 ROADMAP 에 적고 **커밋**
   ② 고친다 → 다시 잰다 → **커밋**. 작업 트리에만 남은 것은 다음 세션에게 **없는 일**이다.

---

너는 necro 리포의 hs/ (직접 조작 핵앤슬래시 프로토타입, 바닐라 JS + canvas)에서 일한다.
기준 문서: `docs/HS_STYLE.md` · `docs/ROADMAP.md`(**맨 아래 「V-196 착수」 블록이 이 판이다 — 먼저 읽어라**).

## 앞선 판이 남긴 것 (다시 파지 마라)
· 밀도/배치 축 **닫힘**. V-193 의 CV 자는 **폐기**(파일 머리 주석 참고) — 다시 쓰지 마라.
· 화면 아래(떠오르는 글자·하단 UI 겹침·소수점) 축 **닫힘**(V-195). 회귀로만 확인해라.
· **주인공이 무리에 묻히는 건 이미 고쳐졌다**(V-153/V-157 의 맨위·금빛테·발밑고리). 확대해 확인했다.
  다시 건드리지 마라.

## 이 판이 할 일 — 몹 머리 위를 고친다. 늘리지 마라.

### ① 자를 세운다: `tools/hs_v196_bars.mjs`
`tools/hs_v195_hud.mjs` 를 본으로 복사해 고쳐라(CDP 9333 · `node tools/chrome_guard.mjs` 를 먼저 부른다).
자동조종 실플레이, 씨앗 1·2, 각 120초, 1초마다 표본. **게임 안 좌표로** 재라(그림 픽셀 짐작 금지).
V-195 가 쓴 수법을 그대로 써라 — 그리는 코드가 **실제로 그린 사각**을 배열에 남기게 하고(`window.__barRects`
같은 것) 자는 그것만 읽는다. 자와 그림이 어긋나면 안 된다.
  ㉠ **바 뜸(px)** — 각 바의 아래끝과 **그 몹 스프라이트의 불투명 위끝** 사이 세로 거리. p50·p95.
     불투명 위끝은 **리포에 이미 있는 `spriteFoot()`** 계열(불투명 픽셀 경계 캐시)로 구해라.
     없으면 같은 방식으로 위끝도 재게 넓혀라 — **매직넘버 금지.**
  ㉡ **바끼리 겹침(%)** — 동시에 그려진 바 쌍 중 사각이 겹치는 쌍의 비율.
  ㉢ **무리 단조로움(%)** — 화면 안 적 중 «(base, dir, 색조) 최빈 조합»이 차지하는 비율.
  회귀: 교전중 적 p50(적≥1 표본의 중앙값) · frame p95 · 콘솔 오류 수 · V-195 의 넷(잘린 글자 ·
  하단 UI 겹침 · 글자겹침 · 소수점) — `hs_v195_hud.mjs` 를 그대로 한 번 더 돌리면 된다.
결과는 `tmp/v196_bars.json`, 컷은 `tmp/v196_t{20,60,120}.png`(씨앗 1).

### ② 고치기 전 값을 잰다 → ROADMAP 에 적고 커밋한다
**바닥이 문턱에서 먼지 확인해라.** 어떤 눈금이 이미 0/100 에 붙어 있으면 그건 눈금이 아니다 —
그 사실을 적고 통과선에서 빼라(억지로 맞추지 마라).

### ③ 고친다 (hs/ 게임 코드)
· ㉠ **바 위치를 `m.h`(이름값) 대신 «그려진 실루엣의 불투명 위끝»에 건다.** 이것이 이 판의 핵심이다 —
  `spriteFoot()` 이 발밑에 한 일을 머리 위에 그대로 한다([[sprite-brings-its-own-ground]]).
  **네임드 바·이름표·잡몹 바 셋 다** 같은 함수를 지나게 해라. 한 곳만 고치면 [[carry-fixes-forward]] 위반이다.
  주인공/소환수에도 머리 위에 뭘 그리는 곳이 있으면 **같이 옮겨라.**
· ㉡ 겹치는 바는 **위로 어긋내라**(간단한 밀어내기. 물리 시뮬 금지). V-195 가 떠오르는 글자에 쓴
  세로 밀어내기와 **같은 결**로 — 가능하면 그 함수를 재사용해라.
· ㉢ **에셋을 새로 굽지 마라.** 가진 그림으로 결을 낸다 — 개체별 미세 색조 흔들기(±, 눈에 띄는
  무지개 금지)·크기 ±·걷기 프레임 **위상 어긋내기**(각 몹이 자기 오프셋을 갖게). 좌우반전이 없으면
  넣어도 좋다(단 방향 스프라이트와 어긋나지 않게 — 어긋나면 넣지 말고 사유를 적어라).
  새 몹 그림이 필요하다는 결론이 나면 **ROADMAP 에 적기만** 하고 다음 판으로 넘겨라.
· `ctx.filter` 로 칠하거나 `fillRect` 로 에셋을 흉내내지 마라(에셋은 픽셀랩 파이프라인 전용).
  ※ 색조 흔들기는 «이미 있는 스프라이트에 거는 tint»라 허용 — **새 형체를 그리는 것**이 금지다.

### ④ 다시 잰다 → 회귀도 같이 본다 → ROADMAP 에 «전/후»를 나란히 적고 커밋·푸시
회귀 자: `node tools/hs_v195_hud.mjs`(V-195 넷) · `node tools/hs_v186_tree.mjs 30 7` ·
`node tools/hs_v190_xp.mjs 60 1`. `node --check hs/main.js`. 콘솔 오류 0 이어야 한다.

## 마지막 응답에 반드시 담을 것
고친 파일 목록 · ㉠~㉢ **전/후 수치** · 회귀 결과 · 커밋 해시(둘 이상) · 남긴 컷 경로.
PROMPT
)"
