#!/bin/bash
# V-183 — 물건은 섰다. 이제 «화면이 꽉 차야» 한다.
#
# V-181/V-182 가 옵션 굴림 · 격자 가방 · 착용 · 비교까지 세웠다(파밍의 뼈대 완성).
# HS_STYLE ③ 은 「적이 수십 마리 동시에 · 몰려오고 몰려서 죽는다 · 네임드는 머리 위에
# 이름표 + 체력바 · 이펙트가 화면을 덮는다」를 규격으로 못박아 두었다.
. "$(dirname "$0")/ab_guard.sh"
cd "$(dirname "$0")/.." || exit 1

python3 /Users/lbs/.openclaw/workspace/tools/opencode_safe_run.py \
  --timeout 2400 --cwd "$PWD" "$(cat <<'PROMPT'
너는 necro 리포의 hs/ (직접 조작 핵앤슬래시 프로토타입, 바닐라 JS + canvas)에서 일한다.
docs/HS_STYLE.md 의 **③ 절**(화면이 꽉 찬다)과 「빛깔·글꼴」 절이 이 판의 규격이다. **먼저 읽어라.**
직전 두 판(V-181 `497d9c8` · V-182 `5c0102c`/`c485166`)이 물건·가방·착용·비교를 세웠다.
그건 **건드리지 마라.** 이 판은 «싸움의 밀도» 하나만 판다.

## ★ 먼저 재라 — 고치기 전에 (짐작으로 손잡이를 돌리지 마라)
지금 `hs/map.js` 는 방마다 팩 1~2개 · 팩당 5~12마리다. 그런데 **실제로 화면 안에 몇 마리가
동시에 보이는지는 아무도 잰 적이 없다.** 먼저 `tools/hs_v183_density.mjs` 를 만들어
CDP(9333, `tools/chrome_guard.mjs` 를 먼저 부른다)로 한 층을 **자동 플레이**시키며 90초 동안
0.5초마다 재라:
  ① 카메라 안(화면 안)에 살아 있는 적 수 — p50 · p95 · 최대
  ② 깨어 있는(awake) 팩 수
  ③ `window.__prof.summary()` 의 frame/sim/draw p95
**이 표를 로그에 먼저 찍어라.** 이게 이 판의 자다 — 고친 뒤 같은 자로 다시 잰다.

## 할 일 — V-183: 몰려온다
### 1) 떼로 몰려오게
- 화면 안 동시 적 p50 이 지금 몇이든, 목표는 **p50 ≥ 25 · p95 ≥ 40**(HS 의 「수십 마리」).
- 손잡이는 팩 크기만이 아니다. **깨우는 반경**(한 팩만 깨어나면 화면이 빈다) · 방당 팩 수 ·
  적의 이동 속도(뒤처진 놈이 안 붙으면 떼가 안 된다)를 **같이** 본다. 어느 손잡이가
  실제로 자를 움직였는지 값으로 적어라 — 안 움직이는 손잡이는 버려라.
- **뭉쳐서 겹치지 않게** — 서로 밀어내는 힘(separation)이 이미 있으면 그걸 쓰고, 없으면
  싸게 넣어라. 스무 마리가 한 픽셀에 포개져 있으면 「수십 마리」가 아니라 한 마리로 보인다.

### 2) 네임드(champion/named)
- 팩 우두머리에 **머리 위 이름표 + 체력바**. HS 는 초록 글씨 대문자(`SLITHER COMMANDER`).
  이름은 굴려서 짓되(형용사 + 종족 + 칭호) 색은 HS_STYLE 「빛깔·글꼴」을 따른다.
- 이미 `m.elite` 가 있다. **다시 만들지 말고 그 위에 얹어라.** 엘리트는 더 크고 느리고
  더 아프게, 죽으면 물건을 확실히 떨군다(이미 그렇다 — 확인만).

### 3) 몰살감 — 죽는 순간이 시원해야 한다
- 여러 마리가 **동시에** 죽을 때 화면이 반응해야 한다: 처치 연쇄(짧은 시간 안에 N마리 죽으면
  화면 흔들림·번쩍임이 커진다), 시체가 쌓인다(이미 `G.corpses` 200개 상한이 있다).
- **연출에 상한을 둬라** — 동시 파티클/떠오르는 숫자 개수 상한 없이 넣으면 프레임이 죽는다
  (`flyText` 의 `flyAlive`, `boom` 의 `fxBusy` 가 이미 그 본이다).

## ★ 프레임 예산 — 이 판의 실패 조건
적을 늘리면 프레임이 죽는다. **frame p95 ≤ 16.7ms(60fps)** 를 지켜라. 못 지키면 밀도를
낮추지 말고 **그리는 쪽을 고쳐라**(화면 밖 적은 그리지 마라 · 그림자·체력바는 가까운 것만 ·
스프라이트 뽑기 캐시). 어디에 시간이 갔는지 `__prof` 의 구간별 값으로 말해라.

## 검증 — 반드시 하고 결과를 로그에 남겨라
1. `node tools/hs_v183_density.mjs` 를 **고치기 전 · 고친 뒤** 두 번 돌려 같은 표를 나란히 찍어라.
   (씨앗을 박아라 — 한 판은 표본 하나다. 최소 씨앗 3개 평균.)
2. 컷 두 장: `tmp/v183_swarm.png`(적이 몰려온 순간 · 네임드 이름표가 보이게) ·
   `tmp/v183_kill.png`(여러 마리가 동시에 죽는 순간).
3. 콘솔 오류 0.

## 마무리
- 검증이 통과하면 **커밋해라**(한국어 한 줄 + 무엇을 왜).
- docs/ROADMAP.md 의 V-183 항목을 만들어 체크하고 **잰 값(고치기 전/후 표)을 그 아래 적어라.**
  못 한 것은 「남음:」 으로 정직하게 남겨라.
PROMPT
)"

echo "DONE V-183"
