#!/bin/bash
# V-184 — 바닥 물건 이름표가 화면 한복판을 덮는다.
#
# V-183 이 떼 밀도를 세우고 컷을 찍다가 눈으로 찾았다: 적 31마리 골에서 바닥
# 이름표가 **열네 개** 겹쳐 쌓여 사람도 적도 안 보인다. 스물여덟 판을 그림·밀도에
# 쓰는 동안 아무 자도 이걸 안 보고 있었다.
. "$(dirname "$0")/ab_guard.sh"
cd "$(dirname "$0")/.." || exit 1

python3 /Users/lbs/.openclaw/workspace/tools/opencode_safe_run.py \
  --timeout 2400 --cwd "$PWD" "$(cat <<'PROMPT'
너는 necro 리포의 hs/ (직접 조작 핵앤슬래시 프로토타입, 바닐라 JS + canvas)에서 일한다.
docs/HS_STYLE.md 를 **먼저 읽어라** — 「빛깔·글꼴」 절이 이 판의 색 규격이다.
직전 판들(V-181 물건/툴팁 · V-182 가방/착용 · V-183 떼 밀도)은 끝났다. **건드리지 마라.**
이 판은 **바닥 물건 이름표가 화면을 덮는 것** 하나만 판다.

## 지금 무엇이 잘못됐나 (실제 코드 위치)
`hs/main.js` 의 `drawItems()` / `drawItemLabel()` (1192~1223줄 근처).
바닥에 떨어진 **모든** 물건이 이름표를 늘 그린다. 겹치면 `while (moved && guard++ < 24)`
루프가 **위로 18px씩 밀어 쌓는다**(최대 8칸). 그래서 물건이 쌓인 방에서는 이름표 기둥이
화면 한복판을 통째로 덮어 사람도 적도 안 보인다. 물건이 쌓일수록 나빠진다.
`itemLabels[]` 는 툴팁 마우스 판정(1243~1280줄)이 쓰므로 **그 계약을 깨지 마라.**

## ★ 먼저 자를 박아라 — 고치기 전에 (짐작으로 고치지 마라)
`tools/hs_v184_labels.mjs` 를 만들어 CDP(9333, `tools/chrome_guard.mjs` 를 **먼저** 부른다)로
한 층을 자동 플레이시키며 재라. **씨앗 3개**(한 판은 표본 하나다) · 각 60초 · 0.5초마다:
  ① **덮음률** — 사람 화면좌표 기준 반경 ±200px 사각 안에서 **이름표 사각이 덮는 화소 비율(%)**.
     (`itemLabels` 를 window 에 노출시켜 읽어라. 겹친 사각은 이중으로 세지 말고 합집합 넓이로.)
  ② 동시에 그려진 이름표 **개수** — p50 · p95 · 최대
  ③ 밀어 올린 **최대 층수**(0 이면 안 밀린 것)
  ④ `window.__prof.summary()` 의 frame p95
**고치기 전 표를 로그에 먼저 찍어라.** 이게 바닥값이다 — 고친 뒤 같은 자로 다시 잰다.

## 할 일 — D2 가 하는 그대로
① **기본으로 감춘다.** 이름표는 평소 안 보인다. 바닥엔 물건이 있다는 **작은 표시**만
   남긴다(지금 있는 금색 점 `arc(sx, sy+8, 3)` 처럼 — 레어도 색으로).
② **ALT 를 누르고 있는 동안** 화면 안 이름표가 **전부** 보인다(D2 와 같다).
   키를 떼면 즉시 사라진다. 창(가방 I)이 열려 있어도 새지 않게.
③ **늘 보이는 하나** — 마우스가 얹힌(또는 사람에게서 가장 가까운) 물건 하나는 ALT 없이도
   이름표가 보인다. 툴팁 판정이 계속 되게 `itemLabels` 를 그때도 채워라.
④ ALT 로 다 켠 상태에서도 **개수 상한**을 둔다(예: 사람에게 가까운 순 12개). 그리고
   **위로 밀어 쌓는 층수를 줄여라**(8층 → 3층). 넘치면 그리지 말고 버린다.
⑤ 떨어진 지 오래된 **흔한(common) 물건**은 ALT 를 눌러도 접는다 —
   레어 이상은 항상 우선(색이 규격이다: HS_STYLE 「빛깔·글꼴」).

## ★ 실패 조건
- 자동 판정: **덮음률이 고치기 전의 1/5 이하**(ALT 안 누른 평상시)여야 통과.
- 물건을 **못 줍게 되면 실패다** — 밟아서 줍는 경로와 툴팁 경로가 그대로 도는지 확인해라.
- frame p95 ≤ 16.7ms 유지(지금 1.7ms 다 — 나빠지면 안 된다).
- 콘솔 오류 0.

## 검증 — 반드시 하고 결과를 로그에 남겨라
1. `node tools/hs_v184_labels.mjs` 를 **고치기 전 · 고친 뒤** 두 번 돌려 같은 표를 나란히 찍어라.
2. 컷 **세 장**(물건이 여러 개 쌓인 같은 자리에서):
   `tmp/v184_before.png`(고치기 전) · `tmp/v184_after.png`(고친 뒤 평상시) ·
   `tmp/v184_alt.png`(ALT 를 누른 상태). 셋 다 **적이 있는 방**에서 찍어라.
3. 툴팁이 여전히 뜨는지, 밟아서 줍는지 각각 한 줄로 확인 결과를 적어라.

## 마무리
- 검증이 통과하면 **커밋해라**(한국어 한 줄 + 무엇을 왜).
- docs/ROADMAP.md 의 V-184 항목을 **체크하고 잰 값(전/후 표)을 그 아래 적어라.**
  못 한 것은 「남음:」 으로 정직하게 남겨라. 새로 눈에 걸린 것은 **V-185 로 새 항목을 열어** 적어라.
PROMPT
)"

echo "DONE V-184"
