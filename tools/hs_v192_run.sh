#!/bin/bash
# V-192 — 여섯 판 연속(V-186~191) 「성장 창」 축만 팠다. 축을 «전투 화면»으로 옮긴다.
#          HS_STYLE ②③ 은 레퍼런스에서 읽어 낸 «수»를 못 박아 뒀다 — 그 수로 지금을 잰다.
. "$(dirname "$0")/ab_guard.sh"
cd "$(dirname "$0")/.." || exit 1

python3 /Users/lbs/.openclaw/workspace/tools/opencode_safe_run.py \
  --timeout 2400 --cwd "$PWD" "$(cat <<'PROMPT'
⛔ 최우선 규칙 — 이걸 어기면 실패다
이 작업은 **무인 배치**로 돈다. 사람이 없고, 네가 턴을 끝내면 **아무도 이어받지 않고 그대로 종료된다.**
1. **서브에이전트/탐색 에이전트를 띄우고 기다리지 마라.** 필요한 파일은 네가 직접 Read/Grep 해라.
   `hs/main.js` 는 1871줄이다 — offset/limit 으로 나눠 읽어라. 위임보다 직접 읽기가 빠르고 확실하다.
2. **턴을 중간에 끝내지 마라.** 「결과가 오면 이어가겠다」·「여기서 마칩니다」 전부 금지.
   한 턴 안에서 탐색 → 자 세우기 → 재기 → 고치기 → 재검증 → 커밋 → push 까지 **끝까지 간다.**
3. **승인·확인 질문 금지.** 답할 사람이 없다. 애매하면 **회귀 위험이 가장 적은 해석**을 스스로
   골라 한 줄로 로그에 남기고 계속 가라.
4. **계획만 있는 응답은 실패다.** 최종 응답에 반드시: 고친 파일 목록 · 검증 수치 · 커밋 해시.
5. **막히면 줄여서라도 완주해라.** 절반만 해도 **동작 + 검증 + 커밋**에 도달한 쪽이 정답이다.
6. 예산 40분. 탐색에 10분 넘게 쓰지 마라.

---

너는 necro 리포의 hs/ (직접 조작 핵앤슬래시 프로토타입, 바닐라 JS + canvas)에서 일한다.

## 이 판이 답할 것 — ★ 축을 바꾼다

직전 여섯 판(V-186·187·188·189·190·191)이 **전부 「성장/스탯 창」 한 축**이었다. 창은 이제 섰다.
그런데 병수님이 실제로 보는 시간의 99% 는 **창이 아니라 전투 화면**이다.

`docs/HS_STYLE.md` 의 ②③ 은 레퍼런스 스크린샷에서 **직접 세어 적은 수**를 갖고 있다:
- **② 바닥이 곧 루팅이다 — ★ 이 게임의 심장**: 한 화면에 **바닥 이름표 여섯 개**가 동시에 떠 있다.
  금은 **알갱이로 수십 개** 흩뿌려지고 **「한 번에 열 개씩」** 떨어진다.
- **③ 화면이 꽉 찬다**: 적이 **수십 마리** 동시에 화면에 있다.

necro 의 지금 값은 **아무도 세어 본 적이 없다.** 이 판은 그것을 **먼저 수로 재고**, 미달인 것만 고친다.
(주의: HS_STYLE ② 본문의 「necro 에는 이것이 통째로 없다」는 **낡은 문장**이다 — V-184 가 바닥
 이름표를, dropLoot 이 금 알갱이를 이미 넣었다. 그러니 「없다」가 아니라 **「얼마나 되나」**를 재라.
 그리고 그 낡은 문장을 고쳐 적어라.)

## ★ 통과선 — ★★ 재기 전에 여기 박아 둔다 (나중에 정하면 자기변호가 된다. 이 수를 고치지 마라)

세 값 모두 **자동조종 실플레이 3분**, 씨앗 둘(1·2), **1초마다 표본**을 떠서 중앙값(p50)으로 낸다.
- **㉠ 화면 안 살아 있는 적 수 p50 ≥ 12** (레퍼런스 「수십 마리」의 보수적 하한)
- **㉡ 화면 안 바닥 이름표 수 p50 ≥ 3** (레퍼런스 6 의 절반)
- **㉢ 처치 한 번당 금 알갱이 수 ≥ 5** (레퍼런스 「한 번에 열 개씩」의 절반)
「화면 안」은 **카메라에 실제로 보이는 사각** 기준이다(월드 전체가 아니다 — 지름길로 세면 그 길에
없는 결함이 안 잡힌다).

## 해야 할 일

### 1) ★ 고치기 전에 `docs/ROADMAP.md` 맨 끝에 「V-192 착수」를 먼저 적어라
축을 왜 바꾸는지(여섯 판 연속 창) · 위 통과선 셋 · 잴 방법. **적고 나서 커밋하고** 다음으로 간다.

### 2) 자 `tools/hs_v192_dense.mjs` 를 세워라
- `tools/hs_v190_xp.mjs` 를 **본으로 삼아라** — 헤드리스 9333 · `chrome_guard.mjs` · 씨앗 틀 ·
  자동조종(`ensureBuild`/`tick`) · `Page.captureScreenshot` 가 거기 다 있다. 복제해서 고쳐 써라.
- **지름길 금지**: 게임 안 상태(`G.enemies`·`G.items`·`G.golds`)를 카메라 사각으로 걸러 센다.
  `G` 접근 방식과 카메라 변수 이름은 `hs/main.js` 를 읽어 **실제 이름을 확인하고** 써라.
- 씨앗별로 1초 표본을 모아 p50/평균을 내고 `tmp/v192_dense.json` 에 적는다.
- **컷도 찍어라**: 판 중 20초·60초·120초·170초 네 시점 → `tmp/v192_t{20,60,120,170}.png` (씨앗 1).
  ★ 나는(다음 감시) 그림을 **눈으로 열어 볼 것**이다. 너는 그림을 못 보니 컷으로 판정하려 들지 마라 —
  **수로만 판정하고**, 컷은 남기기만 해라.

### 3) 재라. 그리고 **미달인 것만** 고쳐라
셋 다 통과면 **고치지 마라** — 「이 축은 이미 규격 안이다」로 닫고, 재는 동안 자가 뱉은 다른 수
(예: 적 최대치·이름표 수명·알갱이 수명)를 로드맵에 적어 다음 판 후보로 남겨라. **없는 결함을
만들어 내지 마라.**

미달이면 그 자리를 고친다. 고칠 때 지켜라:
- **㉠ 적 수가 모자라면** — 한 방에 스폰 수를 늘리는 것보다 **동시 상한·스폰 주기·무리 크기**를
  본다. 단 **프레임 예산 16.7ms 를 넘기면 실패**다(p95 를 반드시 같이 재라).
- **㉡ 이름표가 모자라면** — 지금 이름표가 **얼마나 살아 있다 사라지는지**(수명)와 **어느 등급까지
  뜨는지**를 먼저 보라. 등급 문턱이나 수명이 원인일 수 있다. 원인을 **수로 확인한 뒤** 고쳐라.
  (「항목에 적힌 원인은 짐작이다」 — 증상을 수로 먼저 재고, 고친 뒤 같은 자로 다시 재라.)
- **㉢ 알갱이가 모자라면** — `dropLoot` 의 `grains` 를 본다. 금 총액은 **바꾸지 마라**(알갱이당
  값을 나눠 총액을 유지). 밸런스를 건드리는 판이 아니다.
- 코드 필터(`ctx.filter`)로 칠하거나 `fillRect` 로 그려 때우지 마라. 접지는 불투명 픽셀 아래 경계로.

### 4) 회귀 — 반드시 돌리고 수를 적어라
`tools/hs_v186_tree.mjs`(30 7) · `hs_v187_statval.mjs`(30 7) · `hs_v190_xp.mjs` 셋. 전부 통과해야 한다.
frame p95 · 콘솔 오류 0 · 30초 죽음 0 도 같이 적어라. **회귀가 깨지면 고친 것을 되돌려라.**

### 5) 커밋 + push
「V-192 착수」와 「V-192 완성」 두 커밋. ROADMAP 에 잰 수·판정·까닭을 **표로** 남겨라.
통과선 셋 각각에 ✔/✘ 를 박아라.

## 최종 응답에 반드시 넣을 것
- 잰 수 셋(㉠㉡㉢, 씨앗별 + 합) · 통과선 대비 ✔/✘
- 고친 파일 목록(고칠 게 없었으면 「없음」과 그 까닭)
- 회귀 셋 결과 + frame p95 + 콘솔 오류 수
- 커밋 해시 둘
PROMPT
)"
