#!/usr/bin/env bash
# ── V-227 — 「진척을 재는 자」 두 팔 ────────────────────────────────────────────
# BEFORE = 옛 직선거리(목표 방 중심까지). 굽은 복도에서 제대로 걷는 중에도 4초 안 줄어
#          멀쩡한 접근이 잘렸다 — 층1 거리/방 1051 대 층5 8732, 목표 17번 갈아타고 방 2개.
# AFTER  = 길 위 남은 거리(BFS 경로의 잔여 길이).
# 게임 손잡이·간선 규칙(V-225 AFTER)은 전부 «현재 바이너리»로 고정 — 자만 뒤집는다.
# 끝 조건: 층5 거리/방 ≤ 1600 · 층5 방문 ≥ 50% · 면적 ≥ 8%
#          회귀 = 완주 137~319s · 오류 0 · 몸벽밖 0%
. "$(dirname "$0")/ab_guard.sh"
cd "$(dirname "$0")/.."
export V227_ARMS=1
export V223_OUT=tmp/hs_v227_prog.json
exec node tools/hs_v221_danger.mjs 0.4 5 1,2,3
