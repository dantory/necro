#!/bin/bash
# V-185 — 떠오르는 글이 파밍 중 화면 한복판을 덮는다.
#
# V-184 가 이름표 기둥을 걷고 나니 그 자리에 «떠오르는 글»이 남았다. 감시가 컷을
# 직접 열어 보니 로드맵이 적어 둔 줍기 알림(초록/주황)보다 **흰 피해 숫자**가 훨씬
# 더 덮고 있었다 — tmp/v184_after.png 가운데가 숫자로 꽉 찼다.
. "$(dirname "$0")/ab_guard.sh"
cd "$(dirname "$0")/.." || exit 1

python3 /Users/lbs/.openclaw/workspace/tools/opencode_safe_run.py \
  --timeout 2400 --cwd "$PWD" "$(cat <<'PROMPT'
너는 necro 리포의 hs/ (직접 조작 핵앤슬래시 프로토타입, 바닐라 JS + canvas)에서 일한다.
docs/HS_STYLE.md 를 **먼저 읽어라** — 「빛깔·글꼴」 절이 이 판의 색 규격이다.
직전 판들(V-181 물건 · V-182 가방 · V-183 떼 밀도 · V-184 바닥 이름표)은 끝났다. **건드리지 마라.**
특히 V-184 가 세운 이름표 규칙(평소 감춤 · ALT 로 전부 · 늘 하나)을 **되돌리지 마라.**
이 판은 **떠오르는 글(floats)이 화면 한복판을 덮는 것** 하나만 판다.

## 지금 무엇이 잘못됐나 (실제 코드 위치)
`hs/main.js` 의 `G.floats` — 만드는 자리가 흩어져 있고(191·203·208·225·230·233·236·
436·539·549·565·566·568·607·1392줄), 그리는 자리는 `drawFloats()` (1255줄 근처),
수명은 610~611줄이다.

두 종류가 겹쳐 쌓인다:
① **흰 피해 숫자** (607줄, `spawnFloat` 류). 상한이 **60개**다 —
   `if (G.floats.filter((f) => f.txt).length > 60) return;` (606줄).
   떼를 몰살하는 판에서 숫자 수십 개가 사람 위에 통째로 쌓여 **사람도 적도 안 보인다.**
   ★ 이게 컷에서 제일 크게 덮는 것이다(로드맵 항목은 ②만 적었는데, 감시가 컷을 열어
   보니 ①이 더 심했다 — 둘 다 판정해라).
② **줍기/빌드 알림 글** (초록 「자리 +1」·주황 「소환수 피해 +8%」·물건 이름 539/565/566/568줄).
   몰살판에서 여러 줄이 겹쳐 뜬다.

## ★ 먼저 자를 박아라 — 고치기 전에 (짐작으로 고치지 마라)
`tools/hs_v185_floats.mjs` 를 만들어라. **`tools/hs_v184_labels.mjs` 를 본떠라** — 그게
CDP(9333) + 자동조종 + 씨앗 셋 틀을 이미 갖고 있다. `tools/chrome_guard.mjs` 를 **먼저** 부른다.
**씨앗 3개** · 각 60초 · 0.5초마다 재라:
  ① **덮음률** — 사람 화면좌표 ±200px 사각 안에서 float 글자 사각이 덮는 화소 비율(%).
     **합집합 넓이**로 세라(겹친 것 이중계산 금지). `G.floats` 를 window 에 노출시켜 읽되,
     글자 폭은 `ctx.measureText` 로 실제로 재라 — 짐작 폭 금지.
  ② 동시 float **개수** — 피해 숫자 / 알림 글 **따로** p50 · p95 · 최대
  ③ `window.__prof.summary()` 의 frame p95
**고치기 전 표를 로그에 먼저 찍어라.** 이게 바닥값이다.

## 할 일 — 핵앤슬래시가 하는 그대로
① **피해 숫자 상한을 60에서 확 내려라**(예: 12~16). 넘치면 새로 만들지 말고,
   **큰 값이 작은 값을 밀어내게** 해라(작은 숫자가 큰 한 방을 가리면 안 된다).
② **같은 적에게 연달아 들어간 피해는 한 글자로 합쳐라**(누적해서 커지는 수 하나).
   이게 개수를 가장 크게 줄인다.
③ 숫자를 **적 위에서 띄우되 사람 한복판을 피하게** 흩어라 — 지금 ±10px 만 흔든다(607줄).
④ **알림 글(②종)은 개수 상한 3줄**, 넘치면 오래된 것부터 버린다. 물건 이름은
   V-184 가 세운 레어도 색 규격을 그대로 따른다.
⑤ 수명(`t`)이 필요 이상 길면 줄여라 — 다만 **큰 한 방·유니크는 읽을 시간**을 남겨라.

## ★ 실패 조건
- 자동 판정: **float 덮음률이 고치기 전의 1/3 이하**여야 통과.
- **피해가 안 보이면 실패다** — 때렸는데 수가 하나도 안 뜨면 안 된다. 큰 한 방과
  유니크 줍기는 **반드시 보여야** 한다(이건 눈으로도 확인해라).
- V-184 의 이름표 동작(평소 감춤·ALT·늘 하나)이 그대로여야 한다 — 회귀 금지.
- frame p95 ≤ 16.7ms 유지(지금 1.5ms). 콘솔 오류 0.

## 검증 — 반드시 하고 결과를 로그에 남겨라
1. `node tools/hs_v185_floats.mjs` 를 **고치기 전 · 고친 뒤** 두 번 돌려 같은 표를 나란히 찍어라.
2. 컷 **두 장**, 둘 다 **떼를 몰살하는 순간**(적이 많이 죽는 프레임)에서:
   `tmp/v185_before.png` · `tmp/v185_after.png`. after 에서 **사람이 보여야** 한다.
3. 큰 한 방·유니크 줍기가 여전히 읽히는지 한 줄로 확인 결과를 적어라.

## 마무리
- 검증이 통과하면 **커밋하고 `git push` 까지 해라**(한국어 한 줄 + 무엇을 왜).
- docs/ROADMAP.md 의 V-185 항목을 **체크하고 잰 값(전/후 표)을 그 아래 적어라.**
  못 한 것은 「남음:」 으로 정직하게 남겨라. 새로 눈에 걸린 것은 **V-186 으로 새 항목을 열어** 적어라.
PROMPT
)"

echo "DONE V-185"
