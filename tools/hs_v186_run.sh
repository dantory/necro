#!/bin/bash
# V-186 — HS_STYLE ⑤ 「스킬·스탯 창」이 통째로 없다. 성장이 Z/X 두 키로 끝난다.
. "$(dirname "$0")/ab_guard.sh"
cd "$(dirname "$0")/.." || exit 1

python3 /Users/lbs/.openclaw/workspace/tools/opencode_safe_run.py \
  --timeout 2400 --cwd "$PWD" "$(cat <<'PROMPT'
너는 necro 리포의 hs/ (직접 조작 핵앤슬래시 프로토타입, 바닐라 JS + canvas)에서 일한다.
docs/HS_STYLE.md 를 **먼저 읽어라** — 이 판은 그 문서의 **⑤ 스킬·스탯 창** 절이 규격이다.
「빛깔·글꼴」 절도 읽어라(hud.css 의 D2 룩을 그대로 쓴다 — 새 색을 지어내지 마라).

직전 판들(V-181 물건 · V-182 가방/툴팁 · V-183 떼 밀도 · V-184 바닥 이름표 · V-185 떠오르는 글)은
끝났다. **건드리지 마라.** 특히 되돌리면 안 되는 것:
- V-184 이름표 규칙(평소 감춤 · ALT 로 전부 · 늘 하나)
- V-185 `floatNote()` 규칙(같은 글은 줄을 늘리지 않고 시간만 되살린다 · 알림은 살아 있는 것 위로 22px 씩 쌓는다)
- V-182 가방(6×2·착용 일곱 슬롯)과 `recalc()` 한 문

## 지금 무엇이 없나 (실제 코드 위치)
`hs/main.js` 의 `spendPoint(kind)` (222줄 근처)가 성장의 전부다:
`Z` → 자리 +1 · `X` → 소환 등급 해금, 등급을 다 열면 남는 점수는 전부
「소환수 피해 +8%」(`p.mult.minionDmg *= 1.08`)로 녹는다.
**고를 것이 없으니 빌드가 안 갈린다.** 창도 없고, 「Points Left」도 안 보이고,
찍은 것을 되돌릴 수도 없다.

## 할 일 — HS_STYLE ⑤ 를 그대로
① **창을 연다** — `C` 키(HUD 아래 줄에 이미 `C` 칸이 있다)로 여닫는다. 가방(`I`)과 같은
   결의 패널로, `hs/hud.css` 의 기존 규칙을 재사용해라. **게임은 창이 열려 있어도 돈다**
   (V-181 주석 1393줄의 원칙 그대로).
② **좌측 스탯 여섯** — 힘·민첩·지능·기력·방어·활력. 각각 색이 다른 오각별 아이콘.
   맨 위에 **「Points Left: N」**. 스탯 하나에 **+ 단추**. 스탯은 반드시 `recalc()` 한 문을
   지나 실제 수를 움직여야 한다(무엇이 무엇을 움직이는지 표를 코드 주석에 적어라).
③ **우측 스킬트리 두 갈래** — 네크로에 맞게 **「군세」**(소환 자리·등급·소환수 피해/생명)와
   **「죽음」**(뼈창 피해·시체폭발·저주 계열) 두 갈래. 아이콘 격자를 **선으로 잇고**,
   찍은 점수는 칸 안에, **+ 는 칸 오른쪽 위**에. 앞 칸을 안 찍으면 뒤 칸이 잠긴다.
④ **스킬 툴팁** — V-182 물건 툴팁과 같은 결. 「Receives Bonuses From:」 **시너지 목록**
   (초록 제목 · 항목마다 +N% per level)을 넣어라.
⑤ **하단 단추** — Reset Skills / Reset Attributes / Close. Reset 은 점수를 전부 돌려준다.
⑥ **기존 Z/X 는 살려 둔다**(빠른 손) — 다만 같은 곳(스탯/스킬 자료)을 고치게 옮겨라.
   두 길이 서로 다른 상태를 만들면 안 된다.

## ★ 먼저 자를 박아라 — 고치기 전에
`tools/hs_v186_tree.mjs` 를 만들어라. `tools/hs_v184_labels.mjs` 를 본떠라(CDP 9333 · 자동조종 ·
씨앗 셋 틀이 이미 있다). `tools/chrome_guard.mjs` 를 **먼저** 부른다. 재는 것:
  ① **안 움직이는 손잡이가 없다** — 스탯 여섯 · 스킬 칸 전부를 하나씩 1점 찍고
     **찍기 전/후의 실제 수**(`p.dmg`·`p.maxHp`·`p.slots`·소환수 피해 등)를 비교해
     **움직이지 않은 칸을 이름으로 뱉어라.** 하나라도 안 움직이면 **실패**다
     ([[knob-that-does-nothing]]).
  ② **검수기가 사람과 같은 문으로 지나야 한다** — 내부 함수를 직접 부르지 말고
     **창의 + 단추를 실제로 클릭**해서 찍어라([[probe-must-walk-the-real-path]]).
  ③ **Reset 이 정확히 되돌린다** — 40점을 무작위로 찍고 Reset 한 뒤 모든 수가
     처음 값과 **같아야** 한다(점수도 전부 돌아와야 한다).
  ④ **잠금이 지켜진다** — 앞 칸 0점인데 뒤 칸이 찍히면 실패.
  ⑤ 창을 여닫으며 60초 자동조종: `window.__prof.summary()` frame p95 ≤ 16.7ms · 콘솔 오류 0.

## ★ 실패 조건
- 안 움직이는 스탯/스킬 칸이 **하나라도** 있으면 실패.
- Reset 뒤 값이 처음과 다르면 실패.
- V-182 가방 · V-184 이름표 · V-185 떠오르는 글 동작이 바뀌면 **회귀 실패**.
- frame p95 > 16.7ms 또는 콘솔 오류 > 0 이면 실패.

## 검증 — 반드시 하고 결과를 로그에 남겨라
1. `node tools/hs_v186_tree.mjs` 를 돌려 위 ①~⑤ 표를 로그에 찍어라(움직인 수를 **값으로**).
2. 컷 **세 장**: `tmp/v186_stats.png`(스탯 여섯 + Points Left) ·
   `tmp/v186_tree.png`(스킬트리 두 갈래 · 잠긴 칸이 보이게) ·
   `tmp/v186_tip.png`(스킬 툴팁 + 「Receives Bonuses From:」).
   컷은 **손으로 지은 가짜 상태가 아니라 실제 플레이로 얻은 점수**로 찍어라(V-182b 의 교훈).
3. 창을 연 채 전투가 계속 도는지 한 줄로 적어라.

## 마무리
- 검증이 통과하면 **커밋하고 `git push` 까지 해라**(한국어 한 줄 + 무엇을 왜).
- docs/ROADMAP.md 의 V-186 항목을 **체크하고 잰 값을 그 아래 적어라.**
  못 한 것은 「남음:」 으로 정직하게 남겨라. 새로 눈에 걸린 것은 **V-187 로 새 항목을 열어** 적어라.
PROMPT
)"

echo "DONE V-186"
