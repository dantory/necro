#!/bin/bash
# ★ 긴 측정은 저절로 떨어져 나간다(600초 상한에 잘리지 않게) — tools/ab_guard.sh
. "$(dirname "$0")/ab_guard.sh"
# ══ D-37 · **그 눌림을 «짧고 잦게» 한다** ══ (`__PULSECAP` · 기본 0)
#
#   D-36 이 일곱 갈래 만에 처음으로 **«앞»을 한 톨도 안 건드렸다** — 앞 판당 23.33 →
#   23.33(소수점까지 같다) · 죽음 12 → 12 · 최고층 101%. 방아쇠를 깊이로 옮긴 것은 옳았다.
#   그런데 **뒤 사건이 3.00 → 1.67 로 또 절반**이 됐고, 그 수가 D-35 와 한 글자도 안 다르다.
#   ☞ 까닭은 **눌림이 «길어서» 사건이 하나로 합쳐진 것**이다. 뒤에서 무너져 있던 «시간»은
#     164초 → 232초로 **1.4배 늘었는데 «개수»가 줄었다.** 뒤 층 머무는 시간 15.8초에
#     눌림이 8초 — **한 층에 한 번, 층의 절반 내내** 눌린 채다. 게다가 자는 「그 최고에
#     다시 닿아야 다시 문다」라, 풀려서 머릿수가 옛 최고로 돌아갈 틈이 없으면 **여러 번
#     눌러도 한 번으로 센다**([[threshold-and-ruler-must-match]]).
#   ☞ 그래서 미는 것(자리)도 거는 자리(깊이 21+)도 **그대로 두고 «결»만 바꾼다**:
#     3초 눌리고 **4초 온전히 풀리기**를 되풀이한다(묽기 43% 대 D-36 의 51% — 총 눌린 초는
#     비슷하되 사건은 층당 1 번이 아니라 **2.3 번**). 틈 동안 몫은 **정확히 1** 이다.
#
#   끝 조건 (재기 전에 적는다 · 두 팔: A = 끔 · P5 = __PULSECAP=0.5 · 9분 × 씨앗 1·3·7)
#     ★ 견줄 A 팔은 **D-34·D-35·D-36 의 A 팔과 같은 수**다(씨앗이 같으면 판이 똑같이 구른다):
#       앞 판당 23.33 · **뒤 판당 3.00** · 뒤 회복초중앙 18.2 · 죽음 합 12 · 9분 최고층 29.67.
#     ① **문이 «짧고 잦게» 도는가** — 「잦은눌림(D-37)」 줄에서 넷을 다 볼 것:
#        걸림 ≠ 0 · 판당 눌린 초 **20초 이상**(D-36 은 79) · 걸린 층 평균 **21 이상**
#        (21 미만이면 앞으로 샌 것이다) · **한 번에 3.0초 이하**이고 **걸림이 D-36 의
#        30 번보다 두 배 이상(60 번 이상)**. ★ 마지막 둘이 이 갈래의 정체다 — 총량은
#        비슷한데 «개수»가 안 늘면 D-36 을 이름만 바꿔 다시 돌린 것이다.
#     ② **뒤에 «사건»이 더 서는가(이것이 D 의 물음이다)** — 새 자(절대 자) 뒤(층 21+)
#        판당이 A 팔 **3.00 → 4.5 이상**(1.5배). D-35 는 1.67 · D-36 도 1.67 로 «줄었다».
#        ★ **1.67 을 넘기만 해서는 안 된다** — 넘되 4.5 에 못 미치면 「쪼개니 늘긴 하는데
#          모자라다」이고, 그때는 눈금(ON/OFF)을 바꿔 한 팔 더 잰다.
#     ③ **회복이 «시간»을 갖는가** — 뒤 칸 회복초중앙이 A 팔 18.2 보다 **길 것.**
#        ★ 다만 D-36 의 46.3 보다 **짧아도 진 게 아니다** — 46.3 은 사건이 하나로 합쳐져서
#          길어진 수다. 여기서 볼 것은 「18.2 보다 길고, 그러면서 개수가 늘었나」다.
#     ④ **앞을 한 톨도 안 건드렸는가** — 앞(층 1-20) 판당이 A 팔 23.33 의 **1.1배 이내**,
#        죽음 합이 12 의 **1.1배 이내.** ★ 이 갈래도 앞에 방아쇠가 아예 없다(층 21 아래에서는
#        통조차 안 돈다) — D-36 처럼 **소수점까지 같아야** 정상이고, 움직였다면 샌 것이다.
#     ⑤ **판이 더 얕아지지 않았는가** — 최고층이 A 팔 29.67 의 **85% 이상**(25.2).
#   ★ ②③ 이 서고 ④⑤ 가 살아 있을 때만 기본값을 켠다. 하나라도 어긋나면 ☒ 로 닫고
#     무엇이 어떻게 졌는지를 ROADMAP 에 적는다 — **진 것도 답이다**(D-29~36 이 그랬다).
set -u
cd /Users/lbs/source/personal/necro
MIN=${MIN:-9}; SEEDS=${SEEDS:-1,3,7}
node tools/chrome_guard.mjs || true

for arm in A P5; do
  OUT="tmp/d37_${arm}.log"
  [ -s "$OUT" ] && { echo "== $OUT 이미 있음 · 건너뜀 =="; continue; }
  echo "═════ $arm → $OUT · $(date +%H:%M:%S) ═════"
  if [ "$arm" = "A" ]; then K=""; else K="__PULSECAP=0.5"; fi
  NECRO_KNOBS="$K" node tools/start_probe.mjs "$MIN" "$SEEDS" > "$OUT" 2>&1
  echo "== $arm 끝 · $(date +%H:%M:%S) ==" >&2
done

for arm in A P5; do
  echo ""; echo "═════ $arm (tmp/d37_${arm}.log) ═════"
  grep -A 8 '절대 자' "tmp/d37_${arm}.log"
  grep    '잦은눌림(D-37)' "tmp/d37_${arm}.log"
  grep -A 3 '최고층' "tmp/d37_${arm}.log" | head -8
  grep    '죽음' "tmp/d37_${arm}.log" | head -3
done
