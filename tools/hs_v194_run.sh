#!/bin/bash
# V-194 — V-193 은 얻은 것 없이 밀도 회귀만 깼다. 되돌리고, 밀도가 «무엇에» 흔들리는지 가른다.
. "$(dirname "$0")/ab_guard.sh"
cd "$(dirname "$0")/.." || exit 1

python3 /Users/lbs/.openclaw/workspace/tools/opencode_safe_run.py \
  --timeout 2400 --cwd "$PWD" "$(cat <<'PROMPT'
⛔ 최우선 규칙 — 이걸 어기면 실패다
이 작업은 **무인 배치**로 돈다. 사람이 없고, 네가 턴을 끝내면 **아무도 이어받지 않고 그대로 종료된다.**
1. **서브에이전트/탐색 에이전트를 띄우고 기다리지 마라.** 필요한 파일은 네가 직접 Read/Grep 해라.
   `hs/main.js` 는 1900줄 근처다 — offset/limit 으로 나눠 읽어라.
2. **턴을 중간에 끝내지 마라.** 「결과가 오면 이어가겠다」·「여기서 마칩니다」 전부 금지.
3. **승인·확인 질문 금지.** 답할 사람이 없다. 애매하면 **회귀 위험이 가장 적은 해석**을 골라
   한 줄로 로그에 남기고 계속 가라.
4. **계획만 있는 응답은 실패다.** 최종 응답에 반드시: 고친 파일 · 검증 수치 · 커밋 해시.
5. **막히면 줄여서라도 완주해라.** 절반만 해도 **동작 + 검증 + 커밋**에 도달한 쪽이 정답이다.
6. 예산 36분. 탐색에 6분 넘게 쓰지 마라.

★★ 긴 측정은 «한 번에 하나씩, 백그라운드로» 돌려라 — 직전 판이 시간 상한(rc=124)에 잘렸다.
   `nohup node tools/xxx.mjs ... > tmp/_log 2>&1 &` 로 띄우고 짧은 sleep 으로 나눠 폴링해라.
   **한 호출 안에서 6분 넘게 기다리지 마라.**

★★ 커밋 순서: ① 되돌리기 커밋을 **맨 먼저** 한다 ② 그 다음 잰다 ③ 잰 수를 ROADMAP 에 적어
   **추가 커밋**. 작업 트리에만 남은 것은 다음 세션에게 **없는 일**이다.

---

너는 necro 리포의 hs/ (직접 조작 핵앤슬래시 프로토타입, 바닐라 JS + canvas)에서 일한다.
기준 문서: `docs/HS_STYLE.md` · `docs/ROADMAP.md`(맨 아래 미체크 항목 = V-194 가 이 판이다. 먼저 읽어라).

## 앞선 판이 남긴 것 (그대로 믿지 말고 확인은 하되, 다시 파지는 마라)

V-193(커밋 `7e5eb73`)은 「완성」이라 적혀 있으나 **아무것도 넘기지 못했다**:
CV 0.29→0.31 (통과선 0.35 미달) · 축정렬은 **고치기 전부터** 통과(0.33) · 그리고
**밀도 회귀가 깨졌다: 화면 안 적 p50 14 → 1** (통과선 12).

## 이 판이 할 일 — 딱 두 가지다. 늘리지 마라.

### ① V-193 을 되돌린다 (맨 먼저, 재기 전에)
`git revert --no-edit 7e5eb73` (충돌하면 손으로 되돌려라: `hs/map.js` 의 SPD_JIT·AIM_OFF·
`ox/oy`·`sj`, `hs/main.js` 의 `gx/gy/gd` 블록을 원래대로). `node --check` 둘 다 통과시키고
**바로 커밋 + push.** 메시지 앞머리는 `V-194 되돌림 —`.

### ② 밀도가 «V-193 탓» 인지 «자 탓» 인지 가른다 — 이 갈림이 이 판의 전부다
되돌린 코드로 `node tools/hs_v192_dense.mjs 180 1,2` 를 **그대로** 돌려라(자를 고치지 말고).
· **p50 이 12 이상으로 돌아오면 → 원인은 V-193이다.** 여기서 끝이다. ROADMAP 에 수를 적고 닫아라.
  CV 축을 다시 파지 마라(아래 ③ 만 하고 끝).
· **p50 이 여전히 12 미만이면 → 원인은 «자» 다.** 그러면 자의 눈금을 고쳐라:
  화면 안 적 p50 은 **자동조종의 진행 속도에 통째로 흔들린다**(같은 120초에 방 18·처치 1027 이었다가
  방 35·처치 1408 이 된다 — 방을 빨리 비우면 빈 화면 시간이 길어져 중앙값이 1 로 떨어진다).
  → `tools/hs_v192_dense.mjs` 에 **이동 구간에 안 흔들리는 눈금**을 더해라. 예:
    · **「적 ≥1 인 표본만 모은 조건부 p50」** (교전 중 밀도)
    · **「적 ≥12 인 표본의 비율」** (충분히 붐빈 시간의 몫)
  기존 p50 도 **지우지 말고 같이 찍어라**(과거 판과 비교해야 한다). 그리고 **새 눈금의 바닥값을
  먼저 재서** 로그에 적고, 그 바닥에서 «먼» 자리에 통과선을 놓아라 — 바닥에 붙은 수는 눈금이 아니다.
  ★ 이 경우 **게임 코드는 건드리지 마라.** 이 판은 자를 바로잡는 판이다. 고치는 것은 다음 판이다.

### ③ CV 축을 닫는다 (짧게, 반드시)
통과선 CV ≥ 0.35 는 **도달할 수 없는 수였다**: 0.35/0.52 는 「점」이 겹칠 수 있는 포아송 기준인데,
**충돌 반지름을 가진 물체는 최근접 거리가 r합 근처에 몰려 CV 가 구조적으로 낮다.** 격자 여부를
가르는 자는 축정렬(㉡)이고 그건 **처음부터 0.33(무작위 기댓값)으로 통과**였다.
→ `tools/hs_v193_shape.mjs` **파일 맨 위 주석에** 「이 자의 ㉠ CV 통과선은 폐기됐다(까닭: 위 한 줄).
㉡ 축정렬만 유효하다」를 박아라. 파일 밖에만 적으면 다음 사람이 또 집어 든다.

## 하지 말 것
- 적 이동·속도·스폰 로직을 **새로 손대지 마라**(되돌리는 것 말고). 이 판은 자를 바로잡는 판이다.
- 없는 결함을 만들지 마라. ① 이 끝나 p50 이 돌아오면 **그걸로 통과**다.
- 코드 필터(`ctx.filter`)로 칠하거나 `fillRect` 로 그려 때우지 마라.

## 회귀 (되돌리기 커밋 뒤)
`tools/hs_v192_dense.mjs`(위 ②가 곧 이것이다) · `tools/hs_v186_tree.mjs 30 7` · `tools/hs_v190_xp.mjs`.
뒤의 둘은 짧으니 마지막에 붙여 돌려라. frame p95 · 콘솔 오류 0 도 적어라.
컷을 남겨라: `tmp/v194_t{20,60,120}.png`(씨앗 1). ★ 다음 감시가 **눈으로 열어 본다** —
너는 그림을 못 보니 컷으로 판정하려 들지 말고 **수로만 판정하고** 컷은 남기기만 해라.

## 최종 응답에 반드시 넣을 것
- 되돌리기 커밋 해시
- 되돌린 뒤 밀도 수(씨앗별 p50 · mean · p95 · 처치 수 · 마지막 방 번호) → **「V-193 탓」이냐 「자 탓」이냐 한 줄 결론**
- (자 탓이면) 새 눈금 이름 · 바닥값 · 새 통과선 · 그 근거
- 고친 파일 목록 · 회귀 결과 + frame p95 + 콘솔 오류 수 · 커밋 해시 전부
PROMPT
)"
