#!/bin/bash
# V-198 — 축을 바꾼다: 글자/HUD 넷 뒤에 «연출/에셋». 주력기 뼈창이 흰 선분 하나다.
. "$(dirname "$0")/ab_guard.sh"
cd "$(dirname "$0")/.." || exit 1

python3 /Users/lbs/.openclaw/workspace/tools/opencode_safe_run.py \
  --timeout 2400 --cwd "$PWD" "$(cat <<'PROMPT'
⛔ 최우선 규칙 — 이걸 어기면 실패다
이 작업은 **무인 배치**로 돈다. 사람이 없고, 네가 턴을 끝내면 **아무도 이어받지 않고 그대로 종료된다.**
1. **서브에이전트/탐색 에이전트를 띄우고 기다리지 마라.** 필요한 파일은 네가 직접 Read/Grep 해라.
   `hs/main.js` 는 길다 — offset/limit 으로 나눠 읽어라.
2. **턴을 중간에 끝내지 마라.** 「결과가 오면 이어가겠다」·「여기서 마칩니다」·「에이전트를 기다리겠다」 전부 금지.
3. **승인·확인 질문 금지.** 답할 사람이 없다. 애매하면 **회귀 위험이 가장 적은 해석**을 골라
   한 줄로 로그에 남기고 계속 가라.
4. **계획만 있는 응답은 실패다.** 최종 응답에 반드시: 고친 파일 · 검증 수치 · 커밋 해시.
5. **막히면 줄여서라도 완주해라.** 둘 중 하나만 고쳐도 **동작 + 검증 + 커밋**에 도달한 쪽이 정답이다.
6. 예산 36분. 탐색에 6분 넘게 쓰지 마라.

★★ 긴 측정은 «한 번에 하나씩, 백그라운드로» 돌려라 — 앞선 판이 시간 상한(rc=124)에 잘린 적이 있다.
   `nohup node tools/xxx.mjs ... > tmp/_log 2>&1 &` 로 띄우고 짧은 sleep 으로 나눠 폴링해라.
   **한 호출 안에서 6분 넘게 기다리지 마라.**

★★ 커밋 순서: ① 자를 세우고 **고치기 전 값**을 먼저 잰다 → 그 수를 ROADMAP 에 적고 **커밋**
   ② 고친다 → 다시 잰다 → **커밋**. 작업 트리에만 남은 것은 다음 세션에게 **없는 일**이다.

★★ 픽셀랩(에셋 굽기)이 막히면 — 열쇠·MCP·시간 어느 쪽이든 — **㉠ 을 «구운 에셋» 대신
   포기하지 말고, ㉡ 을 먼저 완주해 커밋한 뒤 ㉠ 의 막힌 지점을 ROADMAP 에 한 줄로 남겨라.**
   ㉠ 을 `fillRect`/`ctx.filter` 로 흉내내서 «고쳤다」고 하는 것은 **실패다**(병수님이 못박은 규칙).

---

너는 necro 리포의 hs/ (직접 조작 핵앤슬래시 프로토타입, 바닐라 JS + canvas)에서 일한다.
기준 문서: `docs/HS_STYLE.md` · `docs/ROADMAP.md`(**맨 아래 「V-198 후보」 블록이 이 판이다 — 먼저 읽어라**).

## 앞선 판이 남긴 것 (다시 파지 마라)
· 밀도/배치 축 **닫힘**. V-193 의 CV 자는 **폐기**(파일 머리 주석 참고) — 다시 쓰지 마라.
· 화면 아래(떠오르는 글자·하단 UI 겹침·소수점) **닫힘**(V-195). 회귀로만 확인해라.
· 몹 머리 위(체력바 위치·바겹침·무리 색조) **닫힘**(V-196). 회귀로만 확인해라.
· 바닥 이름표가 살아있는 것을 덮는 것 **닫힘**(V-197 · 주인공 0.0% · 몹 0.8%). 회귀로만 확인해라.
· 어두운 구역의 적은 **의도된 조명**으로 판정해 접었다(V-197 ㉡). 조명을 다시 파지 마라.
· 최근 넷이 전부 **글자/HUD** 축이었다. 이 판은 **연출/에셋** 이다 — 글자를 또 만지지 마라.

## 이 판이 할 일 — 둘. 늘리지 마라.

### ① 자를 세운다: `tools/hs_v198_read.mjs`
`tools/hs_v197_read.mjs` 를 본으로 복사해 고쳐라(CDP 9333 · `node tools/chrome_guard.mjs` 를 먼저 부른다).
자동조종 실플레이, 씨앗 1·2, 각 120초. **그리는 코드가 실제로 그린 사각을 배열에 남기게 하고
자는 그것만 읽는다**(V-195 `__labels`·V-196 `__barRects`·V-197 `__silRects` 와 같은 수법).

  ㉠ **뼈창 발사체가 «그림»인가** — `main.js:869` 근처가 남기는 발사체 화면사각(`__spearRects`
     같은 것을 새로 남겨라) 안에서 **고유색 수**와 **가로 폭(px)**. 지금은 `strokeStyle "#dfeee0"
     lineWidth 3` 짜리 직선이라 색 1~2 · 폭 3 이다. **통과선: 고유색 ≥ 6 · 폭 ≥ 10px.**
  ㉡ **붉은 고리 겹침(%)** — 적 발밑 붉은 타원 고리가 그린 사각/타원을 배열로 남기고,
     **다른 고리와 겹치는 고리 픽셀(또는 사각 넓이) 비율**을 재라. **통과선: ≤ 20%.**
     ★ **먼저 «무엇인지»부터 확인해라** — 고리가 단순 적 표시인지 · 공격범위인지 · 선택표시인지
     코드 근거를 한 줄로 남겨라. **필요한 정보로 밝혀지면 ㉡ 을 접고 사유를 ROADMAP 에 적어라**
     (그때는 ㉠ 하나만 하고 완주해도 성공이다).
  회귀: 교전중 적 p50 · frame p95 · 콘솔 오류 수 · V-195 넷 · V-196 셋 · V-197 둘
     (`hs_v195_hud.mjs`·`hs_v196_bars.mjs`·`hs_v197_read.mjs` 를 그대로 한 번 더 돌리면 된다).
결과는 `tmp/v198_read.json`, 컷은 `tmp/v198_t{20,60,120}.png`(씨앗 1).
★ **컷은 뼈창이 날아가는 프레임으로 잡아라** — 발사체가 화면에 하나도 없는 컷은 쓸모없다.

### ② 고치기 전 값을 잰다 → ROADMAP 에 적고 커밋한다
**바닥이 문턱에서 먼지 확인해라.** 어떤 눈금이 이미 0/100 에 붙어 있으면 그건 눈금이 아니다 —
그 사실을 적고 통과선에서 빼라(억지로 맞추지 마라).

### ③ 고친다
· ㉠ **뼈창 발사체를 픽셀랩으로 굽는다.** `tools/pixellab/fx.py` 가 본이다(같은 MCP 호출 결·
  `assets/fx/` 에 떨어진다 · 판이 어두우니 **밝고 채도 높게**). 옆으로 나는 **뼈 창** 한 장 +
  가능하면 **맞는 순간**(짧은 임팩트). 8방향이 부담이면 **한 장을 진행각으로 회전**해 그려라
  (`ctx.rotate` — 이건 UI 가 아니라 스프라이트 회전이라 허용된다).
  ★ 구운 것은 먼저 `tmp/` 에 스테이징하고, 옮긴 뒤 경로를 ROADMAP 에 적어라.
  ★ **못 구우면 코드로 흉내내지 말고 막힌 지점을 적어라**(위 최우선 규칙 ★★).
· ㉡ 고리가 실재 결함이면 — «가까운 것만 고리» 또는 «겹치면 흐리게» 중 회귀 위험이 적은 쪽.
  고리는 **UI 그리기**라 fillRect 금지에 안 걸린다.
· 접지는 그림의 **불투명 픽셀 아래 경계**로(`footMetrics`). **매직넘버 금지.**

### ④ 다시 잰다 → 회귀도 같이 본다 → ROADMAP 에 «전/후»를 나란히 적고 커밋·푸시
회귀 자: `node tools/hs_v195_hud.mjs` · `node tools/hs_v196_bars.mjs` · `node tools/hs_v197_read.mjs` ·
`node tools/hs_v186_tree.mjs 30 7` · `node tools/hs_v190_xp.mjs 60 1`.
`node --check hs/main.js hs/sprite.js`. 콘솔 오류 0 이어야 한다.

## 마지막 응답에 반드시 담을 것
고친 파일 목록 · ㉠~㉡ **전/후 수치** · 구운 에셋 경로 · 회귀 결과 · 커밋 해시(둘 이상) · 남긴 컷 경로.
PROMPT
)"
