#!/bin/bash
# V-182 — V-181 이 세운 데이터 위에 «창» 을 올린다.
#
# V-181 에서 물건이 슬롯 일곱 · 옵션 굴림 · sumAffixes · 바닥 툴팁까지 왔다.
# 아직 없는 것: 가방을 여는 격자 창 · 착용 슬롯 UI · SHIFT 비교 · 버리기.
# HS_STYLE ④ 는 「격자 + 여러 칸을 차지하는 물건 · 옆에 부적 전용 칸 ·
# 창 밑에 조작 안내 줄」을 규격으로 못박아 두었다.
. "$(dirname "$0")/ab_guard.sh"
cd "$(dirname "$0")/.." || exit 1

python3 /Users/lbs/.openclaw/workspace/tools/opencode_safe_run.py \
  --timeout 2400 --cwd "$PWD" "$(cat <<'PROMPT'
너는 necro 리포의 hs/ (직접 조작 핵앤슬래시 프로토타입, 바닐라 JS + canvas)에서 일한다.
docs/HS_STYLE.md 의 ④ 절과 「빛깔·글꼴」 절이 이 판의 규격이다. **먼저 읽어라.**
직전 판 V-181(커밋 497d9c8)이 hs/loot.js 에 슬롯 일곱 · 옵션 굴림 · sumAffixes(items) ·
바닥 이름표 툴팁을 세워 두었다. **그 코드를 먼저 읽고 그 위에 얹어라 — 다시 만들지 마라.**

## 할 일 — V-182: 격자 인벤토리(I) · 착용 슬롯 · SHIFT 비교

### 1) 격자 인벤토리 창 (I 키로 열고 닫기)
- D2 그대로 **격자**다. 10칸 × 4줄 정도. 물건마다 차지하는 칸 크기를 슬롯별로 정해라
  (예: 무기 2×3 · 갑옷 2×3 · 투구 2×2 · 장갑 2×2 · 신발 2×2 · 반지 1×1 · 부적 1×1).
- 물건을 넣을 자리를 **왼쪽 위부터 훑어 빈 칸에 놓는다**(겹치면 안 된다). 가방이 꽉 차면
  줍지 못하고 「가방이 가득 찼다」를 알린다.
- 칸 안에 물건 아이콘 대신 **레어도 색 사각 + 줄임 이름**으로 그려도 된다(그림 굽지 마라).
- 창이 열려 있는 동안 마우스를 물건에 얹으면 **V-181 툴팁을 그대로 재사용**해 띄운다.

### 2) 착용 슬롯 UI
- 창 왼쪽(또는 위)에 착용 일곱 칸을 **사람 모양 배치**로: 투구 위 · 목걸이 옆 · 무기 왼 ·
  갑옷 가운데 · 장갑/신발 아래 · 반지. 빈 칸은 슬롯 이름을 흐리게.
- 격자의 물건을 **좌클릭하면 착용**(같은 슬롯에 있던 것은 가방으로 되돌아온다).
  착용 칸의 물건을 좌클릭하면 벗어서 가방으로.
- 모든 착용 변화는 **반드시 V-181 의 `sumAffixes(equipped) → recalc()` 한 문을 지나가야 한다.**
  새 계산 경로를 만들지 마라.

### 3) SHIFT 비교
- 물건에 마우스를 얹은 채 **SHIFT 를 누르고 있으면**, 그 물건 툴팁 **옆에** 지금 착용 중인
  같은 슬롯 물건의 툴팁을 나란히 띄운다(D2 의 비교).
- 겹치는 옵션은 차이를 **초록(+)/빨강(−)** 으로 옆에 적어라(예 「피해 +18% (+6)」).
  HS_STYLE 에 초록·빨강이 없으면 이 두 개만 새로 두되 hud.css 에 변수로 두고 이름을 남겨라.

### 4) 버리기 + 조작 안내 줄
- CTRL+우클릭으로 물건을 **바닥에 버린다**(월드에 다시 떨어져 다시 주울 수 있게).
- 창 밑에 안내 줄: `LMB: 착용/해제 | CTRL+RMB: 버리기 | SHIFT: 비교 | I: 닫기`.

### 5) 자동 착용을 끈다
- V-181 은 밟으면 자동으로 갈아 끼웠다. 이제 창이 있으니 **줍기 = 가방에 넣기**까지만 하고,
  착용은 사람이 고른다. 다만 **가방이 비어 있는 첫 물건**은 편의상 자동 착용해도 된다.

## 규칙
- **hs/ 밖(js/ · old/)은 건드리지 마라.** 새 그림을 굽지 마라 — 이번 판은 코드만이다.
- 기존 조작(WASD · 좌클릭 · Q · E · F · R · 1·2·3 · Z · X · U · T · C)을 깨지 마라.
  창이 열려 있는 동안에는 좌클릭이 **공격으로 새지 않게** 막아라(이게 제일 흔한 버그다).
- 창이 열려 있어도 **게임은 계속 돈다**(D2 처럼). 멈추지 마라.

## 검증 — 반드시 하고 결과를 로그에 남겨라
1. `node tools/hs_v182_bagcheck.mjs` 를 **네가 새로 만들어라**: 격자 담기 로직만 떼어
   ① 물건 40개를 무작위로 넣어 **겹친 칸이 한 번도 없는지** ② 꽉 찬 뒤 더 넣으면 거부하는지
   ③ 착용→해제를 200번 왕복해도 가방 칸 수와 스탯이 **처음으로 돌아오는지**(누수 없음) 확인.
   결과를 표로 찍어라.
2. 화면 컷 두 장 (`tools/chrome_guard.mjs` 를 먼저 부르고, 기존 hs 컷 도구 방식으로 CDP 9333):
   · `tmp/v182_inv.png` — 창이 열려 격자에 물건 여러 개 + 착용 칸이 찬 상태.
   · `tmp/v182_compare.png` — SHIFT 비교 툴팁 두 개가 나란히 뜬 상태(차이 색 보이게).
3. 콘솔 오류 0 을 확인해라. 창을 열고 닫으며 **좌클릭이 공격으로 새지 않는지**도 확인.

## 마무리
- 검증이 통과하면 **커밋해라**(메시지는 한국어 한 줄 + 무엇을 왜).
- docs/ROADMAP.md 의 V-182 항목 체크박스를 채우고 **잰 값(표)을 그 아래 적어라.**
  못 한 것이 있으면 「남음:」 으로 정직하게 남겨라.
PROMPT
)"

echo "DONE V-182"
