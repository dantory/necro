#!/bin/bash
# V-187 — 성장창 스탯 칸의 «현재 값»이 빌드 드롭 배수와 섞여 읽힌다.
. "$(dirname "$0")/ab_guard.sh"
cd "$(dirname "$0")/.." || exit 1

python3 /Users/lbs/.openclaw/workspace/tools/opencode_safe_run.py \
  --timeout 2400 --cwd "$PWD" "$(cat <<'PROMPT'
⛔ 최우선 규칙 — 이걸 어기면 실패다
이 작업은 **무인 배치**로 돈다. 사람이 없고, 네가 턴을 끝내면 **아무도 이어받지 않고 그대로 종료된다.**
1. **서브에이전트/탐색 에이전트를 띄우고 기다리지 마라.** 필요한 파일은 네가 직접 Read/Grep 해라.
   `hs/main.js` 가 크면 offset/limit 으로 나눠 읽어라. 위임보다 직접 읽기가 빠르고 확실하다.
2. **턴을 중간에 끝내지 마라.** 「결과가 오면 이어가겠다」·「여기서 마칩니다」 전부 금지.
   한 턴 안에서 탐색 → 구현 → 검증 → 커밋 → push 까지 **끝까지 간다.**
3. **승인·확인 질문 금지.** 답할 사람이 없다. 애매하면 **회귀 위험이 가장 적은 해석**을 스스로
   골라 한 줄로 로그에 남기고 계속 가라.
4. **계획만 있는 응답은 실패다.** 최종 응답에 반드시: 고친 파일 목록 · 검증 수치 · 커밋 해시.
5. **막히면 줄여서라도 완주해라.** 절반만 구현해도 **동작 + 검증 + 커밋**에 도달한 쪽이 정답이다.
6. 예산 40분. 탐색에 10분 넘게 쓰지 마라. 20분에 구현이 시작 안 됐으면 범위를 즉시 줄여라.

---

너는 necro 리포의 hs/ (직접 조작 핵앤슬래시 프로토타입, 바닐라 JS + canvas)에서 일한다.
`docs/HS_STYLE.md` 의 「⑤ 스킬·스탯 창」 절과 「빛깔·글꼴」 절을 **먼저 읽어라**
(hud.css 의 D2 룩을 그대로 쓴다 — 새 색을 지어내지 마라).

직전 판(V-186 스킬·스탯 창)은 끝났다. 이 판은 **그 창의 결함 둘을 고치는 것**이다.
되돌리면 안 되는 것: V-182 가방/툴팁 · V-184 바닥 이름표 · V-185 떠오르는 글 ·
V-186 창 동작(잠금 · Reset 정확 · 창 열려도 전투 계속 · Z/X 가 같은 상태를 고침).

## ① 스탯 칸은 «그 스탯의 순수 기여»만 보인다 (주된 일)
`hs/main.js` 의 `attrLive(p, key)` (1558줄 근처)가 **통합 배수**를 그대로 뱉는다:
  - `str` → `p.dmgMul` · `int` → `p.minionMul`
`p.minionMul` 은 `p.mult.minionDmg`(파밍 중 주운 **빌드 드롭**) × 장비 × 스킬 × 저주까지
전부 곱한 값이다(578줄). 그래서 V-186 컷에서 **지능 1점인데 `×553.66`** 이 떴다.
좌상단 HUD 는 같은 걸 `피해 ×542.80` 으로 이미 보여 준다 — **두 자리가 같은 수를 다르게
말하면서, 스탯 칸이 「이 스탯이 이만큼 준다」로 오독된다.**

고칠 것: **스탯 칸에는 그 스탯이 실제로 기여한 몫만** 쓴다. `ATTRS` 표(238줄)의 계수를
**단일 출처로** 삼아라 — 계수를 `attrLive` 에 다시 적어 넣지 말고 표에서 읽어라
(같은 수를 두 자리에 적으면 다음에 한쪽만 고쳐진다).
  - 힘 `+{lv×3}%` · 민첩 `+{lv×2}%` · 지능 `+{lv×2}%` · 기력 `+{lv×40}` ·
    방어 `+{lv×1.2}%` (상한 75% 에 닿으면 그 사실이 보이게) · 활력 `+{lv×120}`
  - 0점이면 `+0%` 처럼 **0 도 보인다**(칸이 비면 「없는 손잡이」로 읽힌다).
  - 통합 배수는 **좌상단 HUD 에 그대로 남긴다** — 지우지 마라. 옮기는 게 아니라
    **스탯 칸에서 빼는** 것이다.
  - 스탯 툴팁(있으면)에는 「이 스탯: +N% · 합계: ×M」 처럼 **둘을 나란히** 보여도 좋다.

## ② 덤 — 툴팁이 「Reset Attributes」 단추를 덮는다 (V-188 항목)
`tmp/v186_tip.png` 에서 뼈창 툴팁이 창 오른쪽 위로 떠 Reset 단추 글자를 가렸다.
툴팁은 커서 쪽에 붙되 **단추 줄을 피해** 놓이게 하라(자리가 없으면 아래/왼쪽으로 뒤집기).
V-182 물건 툴팁의 배치 규칙이 이미 있으면 **재사용**해라.

## ★ 먼저 자를 박아라 — 고치기 전에
`tools/hs_v187_statval.mjs` 를 만들어라. `tools/hs_v186_tree.mjs` 를 본떠라
(CDP 9333 · chrome_guard 먼저 · 자동조종 · 창의 + 단추를 **실제로 클릭**하는 틀이 있다).
재는 것:
  ① **한 점 몫이 정확하다** — 스탯 여섯 각각에 대해 0점→1점→2점 으로 **창의 + 를 클릭**하며
     칸 표시 문자열에서 수를 뽑아, **한 점마다 정확히 표의 계수만큼** 늘어나는지 확인.
     하나라도 어긋나면 **실패**. (내부 함수 직접 호출 금지 — 사람이 지나는 문으로.)
  ② **빌드 드롭에 오염되지 않는다** — 검수 중에 `p.mult.minionDmg` 를 **강제로 ×100** 해도
     지능 칸 표시는 **변하지 않아야** 한다. 변하면 실패. (같은 순간 좌상단 HUD 의 통합
     배수는 **반드시 변해야** 한다 — 안 변하면 HUD 를 망가뜨린 것이므로 그것도 실패.)
  ③ **방어 상한** — 방어를 상한(75%)까지 찍었을 때 칸 표시가 상한에 걸린 사실을 보인다.
  ④ **툴팁이 단추를 안 덮는다** — 창 안 **모든 칸**에 커서를 올려 툴팁 사각형과
     Reset/Close 단추 사각형의 `getBoundingClientRect()` 교집합이 **0** 인지 확인.
     겹치는 칸이 있으면 이름으로 뱉고 실패.
  ⑤ **회귀** — V-186 자(`tools/hs_v186_tree.mjs`)를 **그대로 다시 돌려** 손잡이 13/13 ·
     잠금 · Reset 이 여전히 통과하는지 확인. 하나라도 깨지면 실패.
  ⑥ 창을 여닫으며 60초 자동조종: `window.__prof.summary()` frame p95 ≤ 16.7ms · 콘솔 오류 0.

## ★ 실패 조건
- 한 점 몫이 표의 계수와 어긋나면 실패.
- 빌드 드롭을 ×100 했을 때 스탯 칸이 움직이거나, HUD 통합 배수가 안 움직이면 실패.
- 툴팁이 단추와 겹치는 칸이 하나라도 있으면 실패.
- V-186 자가 깨지면 회귀 실패.
- frame p95 > 16.7ms 또는 콘솔 오류 > 0 이면 실패.

## 검증 — 반드시 하고 결과를 로그에 남겨라
1. `node tools/hs_v187_statval.mjs` 를 돌려 위 ①~⑥ 표를 **값으로** 로그에 찍어라.
2. `node tools/hs_v186_tree.mjs 60 7` 도 돌려 회귀 결과를 찍어라.
3. 컷 **두 장**: `tmp/v187_stats.png`(스탯 여섯 칸에 순수 기여가 보이고, 좌상단 HUD 에는
   통합 배수가 그대로 있는 한 화면) · `tmp/v187_tip.png`(툴팁이 단추를 안 덮는 상태).
4. `docs/ROADMAP.md` 의 **V-187 과 V-188 을 체크**하고 **잰 값을 그 자리에 적어라.**
   고치다 새로 눈에 걸린 것이 있으면 **새 항목으로 열어 둬라**(고치지 말고 적기만).
5. `git add -A && git commit && git push`. 커밋 메시지 첫 줄은 한국어 한 문장으로
   **무엇이 달라졌는지**를 적어라.

마지막 응답에 반드시: 고친 파일 목록 · 검증 수치 · 커밋 해시.
PROMPT
)"
