#!/bin/bash
# V-189 — 세 빌드를 같은 씨앗으로 돌려 «갈리는지»를 수로 낸다.
. "$(dirname "$0")/ab_guard.sh"
cd "$(dirname "$0")/.." || exit 1

python3 /Users/lbs/.openclaw/workspace/tools/opencode_safe_run.py \
  --timeout 2400 --cwd "$PWD" "$(cat <<'PROMPT'
⛔ 최우선 규칙 — 이걸 어기면 실패다
이 작업은 **무인 배치**로 돈다. 사람이 없고, 네가 턴을 끝내면 **아무도 이어받지 않고 그대로 종료된다.**
1. **서브에이전트/탐색 에이전트를 띄우고 기다리지 마라.** 필요한 파일은 네가 직접 Read/Grep 해라.
   `hs/main.js` 가 크면 offset/limit 으로 나눠 읽어라. 위임보다 직접 읽기가 빠르고 확실하다.
2. **턴을 중간에 끝내지 마라.** 「결과가 오면 이어가겠다」·「여기서 마칩니다」 전부 금지.
   한 턴 안에서 탐색 → 구현 → 검증 → 커밋 → push 까지 **끝까지 간다.**
3. **승인·확인 질문 금지.** 답할 사람이 없다. 애매하면 **회귀 위험이 가장 적은 해석**을 스스로
   골라 한 줄로 로그에 남기고 계속 가라.
4. **계획만 있는 응답은 실패다.** 최종 응답에 반드시: 고친 파일 목록 · 검증 수치 · 커밋 해시.
5. **막히면 줄여서라도 완주해라.** 절반만 구현해도 **동작 + 검증 + 커밋**에 도달한 쪽이 정답이다.
6. 예산 40분. 탐색에 10분 넘게 쓰지 마라. 20분에 구현이 시작 안 됐으면 범위를 즉시 줄여라.

---

너는 necro 리포의 hs/ (직접 조작 핵앤슬래시 프로토타입, 바닐라 JS + canvas)에서 일한다.
직전 판 V-186~188 로 **성장창**(스탯 여섯 · 스킬트리 두 갈래 · Reset · 툴팁)이 다 섰다.
이 판은 **창을 더 꾸미는 것이 아니다.** 창이 애초에 무엇을 위한 것이었는지를 답한다:

  ★ **다르게 찍으면 다르게 놀리는가?** 안 갈리면 성장창은 장식이다.

`docs/ROADMAP.md` 맨 끝의 「V-189」 항목을 먼저 읽어라.
되돌리면 안 되는 것: V-181 옵션 굴림 · V-182 가방/착용/SHIFT 비교 · V-184 바닥 이름표 ·
V-185 떠오르는 글 · V-186 창 동작(잠금·Reset 정확·창 열려도 전투 계속) ·
V-187 스탯 칸은 순수 몫만 · V-188 툴팁이 단추를 안 덮음.

## ① 자부터 만든다 — `tools/hs_v189_build.mjs` (주된 일)
헤드리스 크롬(포트 9333, 다른 자들이 쓰는 방식 그대로)에서 **실제 게임을 실플레이**로 돌린다.
지름길 금지 — 사람이 지나는 길(창 열고 + 단추 실클릭)로 점수를 찍어라
(★ [[probe-must-walk-the-real-path]]). 셋을 **같은 씨앗**으로 돌린다:

  · **군세형** — 지능 + 소환 자리 + 소환 등급 + 소환수 피해/생명
  · **죽음형** — 힘 + 뼈창 + 시체폭발 + 저주
  · **맷집형** — 활력 + 방어 + 기력

각 빌드에 **40점**(스탯 20 / 스킬 20 쯤, 잠금 규칙이 허락하는 선에서)을 찍고
**같은 시간(예 180초)** 동안 자동으로 굴려 다음을 낸다:

  | 빌드 | 도달 층 | 처치 | 죽은 횟수 | 받은 피해 | 낸 피해(뼈창/시체폭발/소환수 각각) | 레벨 |

★ **씨앗을 박아라** ([[seed-the-probe]]) — 한 판은 표본 하나다. 빌드당 **씨앗 3개 이상**을
돌려 평균과 폭을 같이 내라. dt 가 벽시계면 씨앗을 고정해도 갈린다는 걸 알고 있다
([[same-seed-is-not-same-run]]) — 그러면 **틱 수 고정**(고정 dt)으로 돌려라.

## ② 판정 — 갈리는가
· **낸 피해의 «구성»**이 빌드마다 뒤집히는가? (군세형은 소환수 몫이 제일 커야 하고,
  죽음형은 뼈창+시체폭발이 제일 커야 한다.) 구성이 안 뒤집히면 **빌드가 안 갈리는 것**이다.
· 처치/도달층이 셋 다 **서로 10% 안**에 모이면 역시 안 갈리는 것이다.
· **안 갈리면 계수를 고쳐라** — `hs/main.js` 의 `ATTRS` per/unit 과 스킬 계수를,
  «찍은 쪽이 뚜렷하게 세지고 안 찍은 쪽은 안 세지게». 고친 뒤 **같은 자로 다시 재라**
  (★ [[cause-written-in-the-item-is-a-guess]] — 고쳤다고 말하기 전에 같은 자로 다시 잰다).
· 고칠 때 **한쪽을 밀면 반대쪽이 따라 온다**([[equilibrium-pushes-back]]) — 군세형을 세게
  하려고 소환 상한을 올리면 프레임과 난이도가 같이 움직인다. 프레임 p95 와 죽은 횟수를
  **매 측정마다 같이 적어라.**

## ③ 곁들여 — 성장이 너무 쉬운가 (병수님 08-15 「성장이 너무 쉽게 되는듯」)
같은 판에서 **레벨 곡선**을 적는다: 시작부터 180초 동안 **몇 레벨 · 스탯/스킬 몇 점**이
들어오는지. 40점을 다 쓰는 데 몇 분 걸리는지. 이건 이번 판에서 **재기만** 해라 —
수를 바꾸는 건 다음 판이다(재기 전에 고치면 무엇이 움직였는지 모른다).

## ④ 지켜야 할 것
· 새 그림 0장 — 코드와 자만. (에셋이 필요하면 이 판에서 하지 마라.)
· **콘솔 오류 0** · **frame p95** 를 매 측정에 같이 낸다.
· 회귀: `node tools/hs_v187_statval.mjs 60 7` 과 `node tools/hs_v186_tree.mjs 60 7` 이
  **여전히 통과**해야 한다. 통과 못 하면 되돌려라.
· 헤드리스 크롬이 느려지면 `node tools/chrome_guard.mjs` 를 먼저 돌려라.
· 컷 두 장: `tmp/v189_army.png`(군세형 한창 때) · `tmp/v189_death.png`(죽음형 한창 때) —
  **같은 층·비슷한 시점**으로 찍어 눈으로도 다르게 보이는지 남겨라.

## ⑤ 끝내는 법
`docs/ROADMAP.md` 의 V-189 항목을 **[x] 로 체크하고 위 표(잰 값)를 그대로 붙여 넣어라.**
갈렸으면 「갈렸다 + 수」, 안 갈렸으면 「안 갈려서 계수를 이렇게 고쳤고 다시 재니 이렇게 됐다」.
그리고 커밋 + push. 최종 응답에 고친 파일 목록 · 표 · 커밋 해시.
PROMPT
)"
