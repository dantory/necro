#!/bin/bash
# V-197 — 화면에 있는데 «안 읽힌다» — 바닥 이름표가 주인공을 덮는 것과 어두운 구역의 적.
. "$(dirname "$0")/ab_guard.sh"
cd "$(dirname "$0")/.." || exit 1

python3 /Users/lbs/.openclaw/workspace/tools/opencode_safe_run.py \
  --timeout 2400 --cwd "$PWD" "$(cat <<'PROMPT'
⛔ 최우선 규칙 — 이걸 어기면 실패다
이 작업은 **무인 배치**로 돈다. 사람이 없고, 네가 턴을 끝내면 **아무도 이어받지 않고 그대로 종료된다.**
1. **서브에이전트/탐색 에이전트를 띄우고 기다리지 마라.** 필요한 파일은 네가 직접 Read/Grep 해라.
   `hs/main.js` 는 길다 — offset/limit 으로 나눠 읽어라.
2. **턴을 중간에 끝내지 마라.** 「결과가 오면 이어가겠다」·「여기서 마칩니다」·「에이전트를 기다리겠다」 전부 금지.
3. **승인·확인 질문 금지.** 답할 사람이 없다. 애매하면 **회귀 위험이 가장 적은 해석**을 골라
   한 줄로 로그에 남기고 계속 가라.
4. **계획만 있는 응답은 실패다.** 최종 응답에 반드시: 고친 파일 · 검증 수치 · 커밋 해시.
5. **막히면 줄여서라도 완주해라.** 셋 중 둘만 고쳐도 **동작 + 검증 + 커밋**에 도달한 쪽이 정답이다.
6. 예산 36분. 탐색에 6분 넘게 쓰지 마라.

★★ 긴 측정은 «한 번에 하나씩, 백그라운드로» 돌려라 — 앞선 판이 시간 상한(rc=124)에 잘린 적이 있다.
   `nohup node tools/xxx.mjs ... > tmp/_log 2>&1 &` 로 띄우고 짧은 sleep 으로 나눠 폴링해라.
   **한 호출 안에서 6분 넘게 기다리지 마라.**

★★ 커밋 순서: ① 자를 세우고 **고치기 전 값**을 먼저 잰다 → 그 수를 ROADMAP 에 적고 **커밋**
   ② 고친다 → 다시 잰다 → **커밋**. 작업 트리에만 남은 것은 다음 세션에게 **없는 일**이다.

---

너는 necro 리포의 hs/ (직접 조작 핵앤슬래시 프로토타입, 바닐라 JS + canvas)에서 일한다.
기준 문서: `docs/HS_STYLE.md` · `docs/ROADMAP.md`(**맨 아래 「V-197 착수」 블록이 이 판이다 — 먼저 읽어라**).

## 앞선 판이 남긴 것 (다시 파지 마라)
· 밀도/배치 축 **닫힘**. V-193 의 CV 자는 **폐기**(파일 머리 주석 참고) — 다시 쓰지 마라.
· 화면 아래(떠오르는 글자·하단 UI 겹침·소수점) 축 **닫힘**(V-195). 회귀로만 확인해라.
· 몹 머리 위(체력바 위치·바겹침·무리 색조) 축 **닫힘**(V-196). 회귀로만 확인해라.
· **주인공이 무리에 묻히는 건 이미 고쳐졌다**(V-153/V-157 의 맨위·금빛테·발밑고리). 확대해 확인했다.
  다시 건드리지 마라. 이 판의 ㉠ 은 «스프라이트에 묻힘»이 아니라 «글자 판에 덮임»이다 — 다른 병이다.

## 이 판이 할 일 — «플레이어가 못 보는 것» 둘. 늘리지 마라.

### ① 자를 세운다: `tools/hs_v197_read.mjs`
`tools/hs_v196_bars.mjs` 를 본으로 복사해 고쳐라(CDP 9333 · `node tools/chrome_guard.mjs` 를 먼저 부른다).
자동조종 실플레이, 씨앗 1·2, 각 120초, 1초마다 표본. **그리는 코드가 실제로 그린 사각을 배열에
남기게 하고 자는 그것만 읽는다**(V-195 `__labels`·V-196 `__barRects` 와 같은 수법). 그림 픽셀 짐작 금지.

  ㉠ **이름표가 살아있는 것을 덮는 비율(%)** — 바닥 이름표 사각(`itemLabels`/`window.__labels`)이
     (ㄱ) **주인공** 실루엣 사각 (ㄴ) **몹** 실루엣 사각과 겹치는 프레임 비율. 둘을 **나눠** 재라.
     실루엣 사각은 `spriteFoot()`/`opaqueHeadTop()`(V-196 이 세운 불투명 경계)로 구해라 — **매직넘버 금지.**
  ㉡ **어두운 구역 적의 대비** — 화면 안 적 스프라이트의 «불투명 픽셀 평균 밝기 − 둘레 배경 밝기».
     밝은 구역의 적과 어두운 구역의 적을 **나눠** 재라(구역 판정 기준을 코드에서 찾아 적어라).
     ★ **먼저 «무엇인지»부터 확인해라** — 조명인지, 안개인지, 미탐색 방인지, 그냥 어두운 타일인지.
     코드 근거를 한 줄로 남겨라. **의도된 안개로 밝혀지면 ㉡ 을 접고 사유를 ROADMAP 에 적어라**
     (그때는 ㉠ 하나만 하고 완주해도 성공이다).
  회귀: 교전중 적 p50 · frame p95 · 콘솔 오류 수 · V-195 의 넷 · V-196 의 셋
     (`hs_v195_hud.mjs`·`hs_v196_bars.mjs` 를 그대로 한 번 더 돌리면 된다).
결과는 `tmp/v197_read.json`, 컷은 `tmp/v197_t{20,60,120}.png`(씨앗 1).
★ **컷은 교전중 프레임으로 잡아라.** V-196 의 컷 셋은 전부 적이 없는 순간이라 고침을 그림으로
  확인할 수 없었다 — 적이 근접한 프레임에서 셔터를 눌러라.

### ② 고치기 전 값을 잰다 → ROADMAP 에 적고 커밋한다
**바닥이 문턱에서 먼지 확인해라.** 어떤 눈금이 이미 0/100 에 붙어 있으면 그건 눈금이 아니다 —
그 사실을 적고 통과선에서 빼라(억지로 맞추지 마라).

### ③ 고친다 (hs/ 게임 코드)
· ㉠ 이름표가 **주인공을 덮지 않게** 한다. 결은 둘 중 골라라(회귀 위험이 적은 쪽):
  (ㄱ) 주인공 실루엣 사각과 겹치는 이름표를 **위로 밀어낸다** — V-195 drawFloats 의 세로 밀어내기
       함수를 **재사용**해라(V-196 `pushBarUp` 도 같은 결이다). 새로 짜지 마라.
  (ㄴ) 겹치는 것을 **점으로 접는다** — V-184 가 이미 «점» 모드를 가졌다.
  ★ **V-184/V-192 의 규칙은 깨지 마라: 늘 하나는 보인다 · 레퍼런스는 여섯을 보여 준다.**
  ★ 몹 겹침도 **같은 함수로** 처리해라 — 주인공만 고치면 [[carry-fixes-forward]] 위반이다.
· ㉡ 어두운 구역의 적이 실재하는 결함이면 — **그리는 것과 조명을 일치**시켜라. 보일 것이면
  최소 대비를 보장하고(외곽선·바닥 고리 강조 등), 안 보일 것이면 **아예 안 그린다.** 어정쩡한
  반투명은 답이 아니다. 어느 쪽을 골랐는지 사유를 적어라.
· `ctx.filter` 로 칠하거나 `fillRect` 로 에셋을 흉내내지 마라(에셋은 픽셀랩 파이프라인 전용).
  ※ 글자 판·외곽선·고리 같은 **UI 그리기**는 그 금지에 안 걸린다 — 금지는 «몹/타일 형체»다.

### ④ 다시 잰다 → 회귀도 같이 본다 → ROADMAP 에 «전/후»를 나란히 적고 커밋·푸시
회귀 자: `node tools/hs_v195_hud.mjs`(V-195 넷) · `node tools/hs_v196_bars.mjs`(V-196 셋) ·
`node tools/hs_v186_tree.mjs 30 7` · `node tools/hs_v190_xp.mjs 60 1`.
`node --check hs/main.js`. 콘솔 오류 0 이어야 한다.

## 마지막 응답에 반드시 담을 것
고친 파일 목록 · ㉠~㉡ **전/후 수치** · 회귀 결과 · 커밋 해시(둘 이상) · 남긴 컷 경로.
PROMPT
)"
